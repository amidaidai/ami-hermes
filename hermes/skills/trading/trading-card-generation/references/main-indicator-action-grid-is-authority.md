# 主指标行动格是唯一方向源（2026-08-31 P0纠错）

## 铁律

`data_get_pine_tables(study_filter="SVP+ICT+VWAP+CVD")` 返回的行动格（位置/结论/方向/路径/风控/CVD/OI/协同/结构/磁吸↑↓/前位/现位）是**唯一方向源**。当结论字段含 `⚠冲突` / `⚠未收线` / `C等待` / `等收线` / `等解除` 中任一时，整张卡的方向裁决必须是 **C级等待**，禁止被分析者逻辑改写为 A 级做多/做空。

## 行动格结论关键词判定表

| 关键词 | 等级 | 出卡方向 |
|:---|:---:|:---|
| 副S4降权·仅候选 | **C** | 等待·等解除 |
| ⚠冲突 | **C** | 等待·不可下单 |
| ⚠未收线 | **C** | 等待·等收线 |
| 持续N根 | **C** | 等待·再观察 |
| 副S4降权·仅B/C | **C** | 人工·不可自动 |
| ✗禁做 | **X** | 禁做 |
| A多 / A空 | **A** | 自动授权候选 |
| B多 / B空 | **B** | 人工候选·等触发 |
| B等待（带入场） | **B** | 人工·触发后做 |
| C等待（无入场） | **C** | 等待·不入场 |

**反例（2026-08-31 事故）**：
- 1h 主指标：结论 = `副S4降权·仅候选 ⚠冲突 · 等解除·新`、方向 = `观望`、现位 = `待·反抽nPOC·等MSS↓·13:00定`
- 15m 主指标：结论 = `副S4降权·仅候选 ⚠冲突 ⚠未收线 · 等收线·持续2根`、方向 = `偏多·条件2/10`、现位 = `多·回踩VWAP77708.4MSS↑·13:00定`
- 出卡错误地输出：`★ A 级 · 做空（推荐）`、方案 A 追空 77,560–77,580、方案 B 等反抽——**主指标是 C 级等待，硬给 A 级做空，P0 错误**

**正例**：
```
裁决: ○ C级等待 · ⚠冲突 · 等 13:00 定时 + MSS↑ 确认

触发多: 13:00 定时后 MSS↑ + 站回 VWAP 77,708 + 收线
触发空: 跌破 VAL 77,480 + OI 共识>80% + CVD 转正
```
让用户根据后续行情自己判断方向，**分析者不替主指标拍板**。

## 副指标 Composite 矛盾的辅助判定

- 15m Composite +21 偏多 vs 1h Composite −21 偏空 → **跨周期矛盾，本身就是 C 级信号**
- HALDRO State Pack = 4（降权）→ 副指标失效，**不再做加权**
- Confirm Score < 3 → 弱确认，**降级为 B/C**
- Volume Ratio 0（缩量87%）→ 不支持方向突破

**铁律**：副指标 Composite 跨周期矛盾 = 不论主指标说什么，都不应给 A 级自动授权。

## 现价 vs EMA 排列 vs VWAP 的位置定位

| 现价 vs | 含义 | 状态 |
|:---|:---|:---:|
| 现价 > VAH 77,900 | 突破价值区上沿 | ⚠易回踩 |
| 77,734 < 现价 < 77,900 | 价在 VA 上沿 + VWAP 之上 | 多头修复中 |
| 现价 ≈ VWAP 77,734 | VWAP 临界 | ⏸方向待选 |
| 77,480 < 现价 < 77,734 | 价在 VA 之下 + VWAP 之下 | 空头延续 |
| 现价 < VAL 77,480 | 跌破价值区下沿 | 🎯磁吸↓触发 |

**铁律**：当现价在 VWAP 临界 ±ATR 范围内时，**主动给 C 级等待**，不强行判定方向。

## 行动格 "现位" 字段的精确读法

| 现位字段 | 含义 | 等级 |
|:---|:---|:---:|
| `待·反抽nPOC·等MSS↓·13:00定` | 等 13:00 定时 + MSS 确认 | **C** |
| `多·回踩VWAP·MSS↑·13:00定` | 候选多但等收线 | **C** |
| `多·成交密集区·等放量` | 多但缺量能 | **B-** |
| `空·跌破VWAP·MSS↓·等放量` | 候选空但等放量 | **B-** |
| `价在VA上·已破POC·趋势` | 趋势确认 | **A** |
| `已破·VWAP上·做回踩` | 回踩中 | **A** |

**13:00 / 14:00 / 16:00 / 23:00 等"定时"是主指标对时间窗的判断**——出卡时**必须**在「一句触发」中引用此定时，不能漏掉。

## 出卡验证清单（必须在出卡前自检）

- [ ] 读 `data_get_pine_tables` 主指标行动格了吗？
- [ ] 结论字段含 ⚠/C/等/观望/冲突/未收线 吗？→ 整卡方向必须 = C级等待
- [ ] 副指标 HALDRO = 4（降权）吗？→ 不做副指标加权
- [ ] 副指标 Composite 跨周期矛盾吗？→ 不可给 A
- [ ] 主指标"现位"字段有定时（13:00/14:00 等）吗？→ 必须在「一句触发」中引用
- [ ] 多空双向触发条件都列了吗？→ 让用户自己判断
- [ ] 等级是否标 A/B/C/X？→ 标 C 就不要硬升级到 A
- [ ] 完整性备注是否标了 10 步管线状态？→ 缺步要标 ⚠/✗ 不能假装完整
