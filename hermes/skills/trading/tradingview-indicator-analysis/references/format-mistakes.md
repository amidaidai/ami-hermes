# 格式错误记录

每次审计/修复后记录，防止重复犯。

## ✅ 已修复

| # | 错误 | 正确 | 发现时间 |
|---|------|------|---------|
| 1 | 风控用 `—` 分隔值和解说 | 风控用 `：` | v3→v4 |
| 2 | 环境用 `：` | 环境用 `—` (描述性) | v3→v4 |
| 3 | 子项 3 空格缩进 | 5 空格（对齐 `① ` 前缀宽度） | v4 |
| 4 | 入场/SL/TP 价格不粗体 | 全部 `**粗体**` | v4 |
| 5 | 距离从现价算 | 从入场价算止损/止盈距离 | v4 |
| 6 | R:R 误算（现价→目标） | R:R = (目标-入场)/(入场-止损) | v4 |
| 7 | 裸放数据不解读 | 每行带 `— {解读}` | v3→v4 |
| 8 | 用 `[]` 括号标周期 | 高周期行前缀 `4h `，低周期不标 | v4 |
| 9 | 风控段缺标签冒号 | 全段 `单笔：` `日限：` 等 | v4 |
| 10 | `+` 和 `·` 混用 | 统一 `·` | v4 |
| 11 | 英文缩写裸放 | 全中文（DO→多空分界等） | final |
| 12 | 推送消息缺上下文 | 含上次周期+行动指令 | final |
| 13 | 风控④⑤用双冒号 `止损：DO：说明` | 用 `—` 更可读 | final |

## ⚠️ 仍可能犯

- 环境段催化行忘加 `—` 解读
- 提醒推送忘加「上次周期」
- 监控事件重复触发（MIN_GAP 边界）
