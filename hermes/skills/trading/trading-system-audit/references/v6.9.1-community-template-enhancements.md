# v6.9.1 Community Template Enhancements (2026-06-20)

This file captures the exact community-aligned updates made to `references/master-template-v68.md` during the 2026-06-20 full system audit + strategy review ("全面检查审计...联网社区看看...分析模板，应该要接入什么还差什么，怎么优化").

## Why these changes (community signals)
- ICT/SMC + Order Flow + Volume Profile consensus (2025-2026 X/Reddit/YouTube/Freqtrade-adjacent): **CVD only has value when anchored to liquidity sweeps + key levels**. Isolated CVD or sweeps are noise.
- Gold (XAU) traders repeatedly stress **Kill Zone (London/NY)** as the primary filter for sweeps/absorption.
- BTC traders distinguish **spot vs perpetual CVD** to separate real demand from leverage-driven moves.
- Naked POC / LVN as high-value Volume Profile concepts for transitions.

Gaps identified in prior template:
- Liquidity sweep status not explicit.
- CVD divergence not forced as sub-structure in 博弈.
- No dedicated XAU time filter.
- No Naked POC or sweep confirmation rows.
- No spot/perp differentiation.

## Exact deltas applied (v6.9 → v6.9.1)
1. **环境段**:
   - 新增⑪ 时段（XAU优先）：London/NY Kill Zone — 内 · 优先级高 — 扫荡窗口。
   - BTC专属：现货CVD vs 永续CVD {一致/分化}。

2. **结构段**:
   - ④ 价值：... · Naked POC {有/无}
   - ⑤ 流动性：... · 扫荡 {已扫/待扫}

3. **博弈段** (核心):
   - ③ 订单流锚定结构 重组为 3 子项：
     - 流动性扫荡：已扫上 VAH XXX — 关键位类型
     - CVD背离：价格HH + CVD LH — 假突破预警
     - 现货vs永续（BTC）：现货主导 — 杠杆驱动较弱

4. **5m触发**:
   - 新增④ 流动性扫荡确认：{已扫关键位 + CVD {背离/吸收}} — {高概率反转/确认}

5. **模型触发清单**:
   - 扫流动性回收、突破接受 等明确 "+ 流动性扫荡确认" + "无背离" 作为确认条件。

6. **头部**:
   - 版本 bump v6.9.1
   - 列出 deltas 供追溯。

**输出铁律**（强化）：
- 每次出BTC/XAU分析卡前**必须** `read_file("references/master-template-v68.md")` 确认最新格式。
- 正文零机器字段（已存在）。
- 使用 `—` 破折号解读。
- 头部10段 + 正文五段完整。

## Verification commands used
```bash
python -c "
with open('references/master-template-v68.md') as f:
    content = f.read()
print('Header v6.9.1:', 'v6.9.1' in content)
print('流动性扫荡 in 博弈:', '流动性扫荡' in content)
print('现货vs永续 in 环境:', '现货vs永续' in content)
print('时段（XAU优先）:', '时段（XAU优先）' in content)
print('Naked POC:', 'Naked POC' in content)
print('扫荡确认 in 模型:', '流动性扫荡确认' in content)
"
# Expected: all True
```

After patch: `git add references/master-template-v68.md && git commit -m "enhance: v6.9.1 template per community feedback" && git push`

## Implementation notes for code
- Update `auto_card.py` (and monitor render) to populate the new sub-fields.
- In `multi_model_engine.py` or data bridge: expose `liquidity_sweep_detected`, `cvd_divergence_type` (e.g. "价格HH+CVD LH"), `kill_zone_active` for XAU, `naked_poc_present`, `spot_perp_cvd_divergence`.
- Next analysis cards must demonstrate the new structure.

## Future maintenance
When community evolves (new 2026 best practice), repeat: research → read current master-template → patch with explicit sub-items → verify keywords + card regen + 0 leaks → git lock.

This file is the condensed record for this class of "community → template" work. Do not mirror full articles; keep actionable deltas + verification.
