# v6.9.2 Perfect Community Fusion Optimization Pattern (2026-06-20)

## Trigger
User: "动用我配置的技能全面的联网社区完美的优化" or "全部一起给我最完美的优化，开始做吧" or "补充完善，按照你的建议来，完善" after community review of analysis strategy/template.

## Core Workflow (Class-Level)
1. Load governing skills: trading-system-audit, tradingview-indicator-analysis, web-access.
2. Community research: web_search + x_search + web_extract on "ICT SMC CVD order flow volume profile VAH VAL POC liquidity sweep Kill Zone 2026 gold XAUUSD BTC".
   - Extract soul concepts: Liquidity Sweep + CVD divergence/absorption + Displacement.
   - XAU specific: Asia range → London sweep → NY confirmation + Kill Zone filter.
   - BTC specific: spot vs perp CVD differentiation.
   - Backtest reality: 50-65% win rates, R:R ≥2 floor, ≥20 real samples before weight changes.
3. **Iron Law #1**: Always `read_file("references/master-template-v68.md")` first to load current v6.9.2 bottom plate. Never rely on memory or old examples.
4. Patch template (v6.9.2 deltas):
   - Header: bump version + list community basis.
   - Environment: add ⑪ 时段（XAU优先） with London/NY Kill Zone logic.
   - Structure: Naked POC + 扫荡 {已扫/待扫}.
   - 博弈: 强化③ "订单流锚定结构（社区最高概率确认）" with sub-items for sweep @ key level (VAH/VAL/POC/OB/FVG), CVD背离/吸收, present spot/perp for BTC, Displacement strong/weak. Add ⑫ high-prob sequence for XAU.
   - 5m触发: ④ "流动性扫荡 + CVD确认" including Displacement.
   - Model checklists (sweep reclaim, breakout): require + CVD背离 + Displacement.
5. Implement code support (reusable):
   ```python
   def _compute_perfect_signals(engine_data: dict, symbol: str, price: float) -> dict:
       # Kill Zone (XAU priority)
       # Sweep detection from levels
       # CVD divergence from cvd dir + price extremes
       # Displacement from recent 5m chg >0.3%
       # Confluence score: sweep3 + cvd2 + kill2 + disp1 (max 8/8)
       return { "liquidity_sweep": ..., "cvd_divergence":..., "displacement":..., "kill_zone":..., "confluence": "X/8 — 高概率" }
   ```
   - Call in auto_card.py render and multi_model_engine output.
   - Inject into generated cards + monitor_levels meta.
6. Verification bundle (mandatory, real execution):
   - read_file template
   - python hermes/scripts/auto_card.py BTCUSDT && XAUUSD
   - grep -E "setup_id|model_id|entry_tag|exit_tag|critical|warning|info" data/auto_card_*.md → must be 0
   - python -m pytest tests/ -q (target 98+ pass)
   - Check perfect signals in output (Liquidity Sweep 是灵魂, Kill Zone, Confluence)
   - git status --short → add/commit/push (user "锁定" = committed+remote)
7. Update this skill + tradingview-indicator-analysis + linked references.
8. Bootstrap closed-loop: use 成交记录 + 复盘 + 策略治理 with sim trades to accumulate samples.

## Evidence from 2026-06-20 session
- BTC perf: {'liquidity_sweep': '已扫 VAH回收扫荡', 'cvd_divergence': '价格 + CVD 卖 背离/吸收', ..., 'confluence': '5/8 — 高概率'}
- XAU perf: strong displacement + 8/8 confluence + Kill Zone handling.
- Cards enhanced with full iron laws.
- 98 pytest, git pushed f0d4f33.

## Pitfalls
- Never skip read_file template (user corrected 2026-06-19).
- Community input must map to existing modules (scoring/five_model etc.), not add new hype.
- "全部一起" means batch all changes + full bundle in one pass, report real tool output (not plans).
- Zero machine fields in rendered cards is non-negotiable.
- Update both audit skill and indicator-analysis skill when template evolves.

## Cross References
- tradingview-indicator-analysis (template rules + fusion workflow)
- references/execution-closed-loop-and-governance.md (sim trade + governance)
- master-template-v68.md (current v6.9.2 authority)

This pattern is now class-level for future "perfect optimization" requests.