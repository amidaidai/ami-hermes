# v7.3 社区审计修复全记录 · 2026-06-21

## 背景

v7.2 六大社区联网审计发现 20条共识·16已对齐·3差距·1缺失。用户要求"p1p2都做"→全量实施。

## 六源社区共识来源

| 共识 | 出处 | 证据 |
|------|------|------|
| 止损=对手方流动性后方 | X/ICT 2026 | "SL behind opposing liquidity pool, not at structural levels" |
| 连亏→半仓→暂停 | Reddit r/algotrading | "3 losses → halve, 5 losses → pause" |
| 固定分数不跟余额涨 | Freqtrade/NautilusTrader | "Fixed fractional: risk stays % of INITIAL balance" |
| 冰山=碎单+价滞 | Bookmap | "Iceberg: many small lots + price not moving" |
| 多资产相关≤0.7阈值 | NautilusTrader | "Correlation matrix check before multi-position" |

## 五改动详情

### P1-1 止损升级→对手方流动性池后方

**文件**: `hermes/scripts/auto_card.py` — `_calc_stop_target_atr()`

**旧逻辑**:
```python
# Short: 止损在最近阻力上方（零售猎杀区）
stop_structural = above_levels[0][0]  # 锚定结构位本身
stop = max(p + atr_stop_dist, stop_structural)
```

**新逻辑 (v7.3)**:
```python
# Short: 止损在最近阻力后方（对手方流动性池+ATR缓冲）
if above_levels:
    stop_structural = above_levels[0][0] + atr_buffer  # 阻力上方推0.5ATR
    stop = max(p + atr_stop_dist, stop_structural)
    stop_reason = f"{above_levels[0][1]}({above_levels[0][2]})+缓冲上方"

# Long: 止损在最近支撑后方
if below_levels:
    stop_structural = below_levels[0][0] - atr_buffer  # 支撑下方推0.5ATR
    stop = min(p - atr_stop_dist, stop_structural)
    stop_reason = f"{below_levels[0][1]}({below_levels[0][2]})-缓冲下方"
```

**效果**: 止损从"猎杀区"推到"猎杀区后方"，smart money扫流动性时不会被止损。

### P1-2 连亏自动缩仓

**文件**: `hermes/scripts/auto_card.py` — `_consecutive_losses()` + `_adaptive_risk()`

**实现**:
```python
def _consecutive_losses() -> tuple[int, bool]:
    """读取 trade_events.jsonl 统计最近连续亏损笔数"""
    # 倒序遍历 trades，直到遇到盈利 → 打破连亏链
    count = 0
    for line in reversed(lines):
        pnl = float(ev.get("pnl_usd") or 0)
        if pnl < 0: count += 1
        else: break  # Won trade breaks the streak
    return count, (count >= 5)

def _adaptive_risk(engine_data):
    consec, paused = _consecutive_losses()
    if paused: return 0.0  # ≥5笔 → 暂停
    shrink = 0.5 if consec >= 3 else 1.0  # ≥3笔 → 半仓
    raw_risk = r["risk_usd"] * shrink
```

### P1-3 余额膨胀守卫

**文件**: `hermes/scripts/auto_card.py` — `MAX_RISK_USD_CAP`

```python
INITIAL_BALANCE = 100.0  # 棠溪本金
MAX_RISK_USD_CAP = INITIAL_BALANCE * 0.01  # 1%硬上限

def _adaptive_risk(engine_data):
    raw_risk = min(raw_risk, MAX_RISK_USD_CAP)  # 守卫②
    return min(raw_risk, 10.0)  # 守卫③: 10U铁律
```

### P2-1 CVD冰山检测

**文件**: `scripts/vwap_ema_cvd_engine.py` — `analyze_cvd_iceberg()`

**检测特征**:
- 单方向笔数占比 >65% AND 小额碎单 → 疑似冰山
- 有价格数据 → 价格停滞<0.3% → 置信85%
- 无价格数据 → 置信60%

**输出**: `{iceberg: bool, pattern: str, confidence: int, dominant_pct: float, avg_lot_size: float}`

### P2-2 多资产相关性检查

**文件**: `scripts/vwap_ema_cvd_engine.py` — `check_correlation_warning()` + `check_correlation_btc_xau()`

**功能**:
- `check_correlation_warning()`: BTC+XAU双持时警告合并风险
- `check_correlation_btc_xau()`: Pearson相关48根4h·8天窗口
- 阈值: <0.3弱可独立·<0.5中等观察·<0.7较强缩减·≥0.7严重合仓

## 验证

```bash
python -m pytest tests/ -q --tb=short -k "not test_watchdog_ratelimit"
# → 101 passed, 3 deselected

python -c "compile(open('hermes/scripts/auto_card.py').read(),'ac','exec')"  # OK
python -c "compile(open('scripts/vwap_ema_cvd_engine.py').read(),'eng','exec')"  # OK
```

## Commit

`00cc113` — feat: 社区联网审计修复 v7.3 · P1+P2全量实施
