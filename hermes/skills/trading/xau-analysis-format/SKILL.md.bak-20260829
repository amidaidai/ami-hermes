---
name: xau-analysis-format
description: 棠溪分析卡格式约束 — 同步所有渠道的权威格式源。10段头部+五段正文+七行完整操作段。装载此skill后任何对话（Telegram/Discord/CLI）输出自动对齐棠溪标准。
---

# 棠溪分析卡 · 完整格式模板（同步所有渠道）

将此 skill 加载到任意对话后，输出分析卡**必须**遵守以下全套格式。

## ① 品种写法

`{品种} · {交易所}` — **禁止** `交易所：` 前缀

- 加密 → `BTCUSDT.P · BINANCE`
- 黄金/外汇 → `XAUUSD · OANDA`
- 股票 → `AAPL · NASDAQ`
- 期权 → `AAPL250117C · OPRA`

## ② 完整卡骨架

### v9.9 Telegram 手机驾驶舱覆盖规则（2026-07-08）

旧 10段+五段正文适合作为长文档存档；Telegram 正式推送与手机阅读以 `D:/Hermes agent/references/master-template-v68.md` v9.9 为权威：首屏必须结构位前置（上方结构｜⚖现价｜下方结构），完整卡固定 4 表（①周期体温/多周期定位、②关键位/结构关键位、③多源验证/双指标、④最推荐方案），快速卡也必须保留 D/4h/1h/15m/5m、SVP主驾驶、HALDRO副驾驶、订单流三行。裁决只有一个 `⭐主推`，`🔁备选` 只是主推失效路径。

完整卡 ≥ 下述结构，不可简化第五段。极简卡仅用于跟踪（变化 < 0.3% 时）。

```
**① 品种：{品种} · {交易所}**
**② 周期：**
**5m {状态} — {原因}**
**15m {状态} — {原因}**
**1h {状态} — {原因}**
**4h {状态} — {原因}**
**③ 现价：`{price}` · 高 `{high}` · 低 `{low}` · 日变动 {n}%**
**④ 状态：{A做多/A做空/B等待/X禁做/C等待}**
**⑤ 模型：{五类中文名/无}**
**⑥ 评分：{总分}/13 · {扣分原因}**
**⑦ 决策：{一句话} · 置信 {n}/5 — {理由}**
**⑧ 仓位：{正常/半仓/轻仓/禁止} · 最大风险 `{risk}U`**
**⑨ 失效：`{失效价}` · 有效期 {expiry}**
**⑩ 数据：{A/B/C} · {来源数}源 · 多空{级} · Taker{级} · CVD{级}**

**1. 环境**
价格现状+重大催化+DMI TV行+日历事件

**2. 结构 — 逐周期分解**
4h→1h→15m→5m 逐周期独立分析

**3. 博弈**
多空逻辑·分歧·三源一致判断

**4. 操作 — 预案A · {方向}**
① 方向：
② 入场：`{price}` 限价
   确认：{条件1} · {条件2} · {条件3}
③ 风控：{杠杆}x
   止损：`{price}` {距离}
   止盈：`{price}` {R:R1}
   止盈：`{price}` {R:R2}
④ 仓位：{qty} · 风险 {amount}U
⑤ 退出：
   失效：{条件}
   预案：{备用方案}
⑥ 轨迹：{前态} → {现态} · {变化}

**4. 操作 — 预案B · {方向}**
① 方向：
② 入场：`{price}` 限价
   确认：{条件1} · {条件2} · {条件3}
③ 风控：{杠杆}x
   止损：`{price}` {距离}
   止盈：`{price}` {R:R1}
   止盈：`{price}` {R:R2}
④ 仓位：{qty} · 风险 {amount}U
⑤ 退出：
   失效：{条件}
   预案：{备用方案}
⑥ 轨迹：{前态} → {现态} · {变化}

**5. 风控**
① 单笔：{金额} · {仓位等级}
② 日限：{上限}
③ 移损：+1.5R移保本
④ 止损：多`{price}` / 空`{price}`
⑤ 止盈：A→`{price}`·`{price}` / B→`{price}`·`{price}`
⑥ R:R：{比率}
⑦ 心态：{当前心态}
```

> 注：正文零机器字段（禁 setup_id/model_id/entry_tag/exit_tag）。机器字段只存 meta dict 不渲染。

## ③ 格式铁律

分析卡（long-form 10段+五段）：
- 序号 — 纯①②③ · 禁方括号/引用块 `>`
- 价格 — 全部反引号 `4,185`
- 分隔 — `—` 破折号 · 禁 `|`
- 排版 — 一行一信号 · `·` 分隔多维信息
- emoji — 仅用于分析卡等级色点 🟢🔵🟡🔴 · **推送告警卡片禁 emoji**
- 分隔线 — **禁止** — 空行自然分段
- 子项 — 操作段子项缩进3空格无序号
- 模型名 — 只写中文（VWAP反抽/VAH回收/VAL回收/POC拒绝/扫流动性回收/突破接受）
- 英文术语 — 禁 Silver Bullet/Taker/OI/LS/F&G/KillZone 在卡体。全部中文化：银弹窗口/主动买卖/持仓/多空比/恐惧贪婪/时段名。VWAP/CVD/EMA/DXY/Funding/Spot/ADX 保留英文

推送告警卡（cron/push卡片，额外规则）：
- 首行方向标注 — `↑做多`/`↓做空`/`○等待`/`×禁做` + 品种 + **核心结构位** + 现价 + 直接结论 + 中文时间。首行结构位优先放 POC/VWAP/VAL/VAH/DO/周高低中最关键的2-4个，例如：`○ BTC · POC 61,713 / VWAP 61,746 / VAL 61,497 · 现价 61,890 · 回踩已到但确认不足 · 2026年7月3日14：33`。
- 默认格式 — **首行结论 + 1-2句人话解释 + 编号小标题 + 真Markdown管道窄表 + 最终裁决**。不要全是表格，也不要纯文字；每块用 `## ① 当前状态`、`## ② TV信号`、`## ③ 交易预案` 等有序标题，标题与表格之间留空行。
- Telegram 必须通过 Bot API 10.1 `sendRichMessage` + RichMarkdown 发送，不能用普通 `sendMessage`/`parse_mode=MarkdownV2` 冒充；发送后用回执确认 `rich_sent` 或 `rich_message.blocks[*].type == "table"`。详细发送/验证规则见 `references/telegram-rich-tables.md`。
- 禁止假表格/文字对齐/4列以上宽表/长段落/尾注总结/“分析完成”前后缀；表格必须有表头行与 `|:----|` 分隔行
- 禁 markdown 加粗 — 全文无 `** **`
- 英文中文化同上 — Taker→主动买卖，OI→持仓，LS→多空比，KillZone中文化。VWAP/CVD/EMA/ADX/Funding/Spot保留英文
- TV截图 region=full 含右侧价格轴+底部CVD面板，通过 MEDIA:路径 直发首行或按渠道先图后卡

推送告警卡模板范例：
```
↑做多/↓做空/○等待/×禁做 {grade} {price:,} · VWAP {vwap:,} · {time}
① 方向：{bias} · 处理：{treatment}
   趋势 {trend_l}/{trend_s} · 反转 {rev_l}/{rev_s}
② DMI ADX {adx} · CVD {cvd:+,.0f} · {cvd_state}
③ VWAP {vwap:,} — VAH {vah:,} — VAL {val:,} — B2上 {b2h:,} — B2下 {b2l:,}
④ 位置：{pos} · 主动买卖 {taker:.2f} · 持仓 {oi}K
⑤ {treatment} · EMA {ema9:,}/{ema21:,} · {ema_state}
```

cron prompt 铁律：任何推送 Telegram/飞书/Discord/本地报告 的 cron prompt 必须内嵌上述格式铁律（缩略版），禁止让子agent自由发挥格式。示例：
```
**格式铁律**：
- ✅ 首行结论 + 恰好3张真实Markdown管道表
- ✅ 每表≤3列，必须有表头和分隔行
- ❌ 禁假表格/文字对齐/4列以上宽表/长段落/尾注
- ❌ 禁 markdown 加粗 `** **`
- ✅ 首行标注 ↑做多/↓做空/○等待
- ✅ 价格用反引号 ``
- ✅ 中文术语：Taker→主动买卖，OI→持仓，CVD→成交量差值
- ✅ 只输出卡片正文，无“分析完成”
```

## ④ 形态继承规则（全量→精简）

### 多源全量原则（不裁剪指标）

棠溪已明确否决“固定核心因子/减少指标”。格式层要保证：**卡片读起来短，但分析底层不减配。**

- 第一轮正式分析：允许全源采集后输出完整卡。
- 后续跟踪：输出3-5行变化，但不得因此跳过TV主/副指标、CVD/OI、关键位和截图。
- 卡面只展示最有交易意义的冲突点、主倾向、破/守触发、失效位；其余数据留在判断过程，不把用户淹没。
- 情绪/宏观/事件/Depth 不作为“核心因子”删除，只按职责进入过滤、挑战、降级或仓位调整。

### 触发规则

同一品种在同一会话中被连续分析时（用户说"继续分析""跟踪""更新""现在怎么样"），
分析卡形态随分析次数自动降级：

| 次序 | 形态 | 内容 |
|------|------|------|
| 第1次（初入品种） | **完整卡** | 头部10段 + 正文5段 + 全量操作 |
| 第2次起（跟踪刷新） | **精简卡** | 4-8行浓缩 + 保留A/B完整操作 |
| 价格大幅移动≥0.5% 或 DMI等级变过 | **完整卡** | 重新全量 |

### 精简卡格式

```
`{SYMBOL}` {STATUS} · {BIAS}
现价 `{PRICE}` · CVD {CVD} · TV {DMI_GRADE}
DMI演变：{PREV_GRADE} → {CURRENT_GRADE} · {变化原因}

—— A {DIR_A} ——
入场 `{ENTRY_A}` 止损 `{STOP_A}` 止盈 `{TARGET_A}` 1:{RR_A}
—— B {DIR_B} ——
入场 `{ENTRY_B}` 止损 `{STOP_B}` 止盈 `{TARGET_B}` 1:{RR_B}

防守 {DEFENSE} · 风控 {RISK}U · {LEVERAGE}
```

### 精简卡范例（XAU 跟踪）

```
XAUUSD C等待·偏空
现价 `4,180` · CVD`-3,374` · TV C·空1h·下方控制
DMI演变：C·震荡1h→C·空1h · 位置从上方→下方控制，偏空强化

—— A 反抽VWAP不破做空 ⚠ ——
入场 `4,181` 止损 `4,195` 止盈 `4,161`/`4,138` 1:1.4/3.0
—— B VWAP支撑反弹做多 ——
入场 `4,180` 止损 `4,170` 止盈 `4,205`/`4,216` 1:2.5/3.6

防守 VWAP`4,181`最终战 · 风险1.4U · 1000x
```

### 周期继承原则

- **长周期方向（4h/1h）**：每次全量分析时重新评估并缓存结论
- **短周期触发（5m/15m）**：跟踪时只刷新短周期状态，长周期方向继承上次完整卡结论
- **异常检测**：如果跟踪时发现长周期方向与前次结论冲突（如4h从偏多→偏空），自动升级为完整卡重新评估

精简卡保留 **A/B操作段完整**（入场/止损/止盈/R:R/方向），仅压缩环境/结构/博弈/风控。不可精简操作段。

- **B等待不给具体入场/止损/止盈价** → 写触发条件描述
- 价格实打实在关键位附近（差距 < 0.3%） → 必须给出两种走法（破位/守住），让棠溪选
- 不可写"等待方向确认"这种空话 — 要说清"在等什么"

## ⑤ R:R 硬底线

- 每预案至少一个止盈满足 R:R ≥ 1:2
- GO/NO-GO 以主线计划 `rr_a/rr1` 为准，不能用反向预案 `rr_b/rr2` 的较好R:R掩盖主线不足
- 主线 R:R < 1:2 → 必须显示 `NO-GO · rr_ratio`；即使反向预案 R:R ≥ 1:2，也只能写“反向待触发”，不能给总体 GO
- 不满足 → 调入场/止损而非放宽底线
- 仍不满足 → 删该止盈目标或降级观察

## ⑥ 预案A/B对称

- 预案A和预案B必须有完全相同数量的字段行
- 结构对称：方向/入场+确认/止损/止盈/仓位/退出/轨迹
- 不允许A详细B缩水

## ⑦ 操作段完整性（所有资产强制）

所有资产的操作段必须完整展开 ①-⑦：
①方向  ②入场（含≥2确认条件）  ③风控（止损+≥2止盈）  ④仓位（手数+风险U）  ⑤退出（失效+预备方案）  ⑥轨迹（前态→现态）

## ⑧ 数据等级 — 木桶原理

按最弱一环降级：
- Taker C 或不可用 → 总最高 B
- Taker C + CVD C → 总 C
- 不可单项 A 就标整体 A

## ⑨ 附属规则

- **TV MCP 第一优先**：所有数据分析**必须先调用 TV MCP 工具** `chart_get_state` → `data_get_study_values` → `data_get_pine_lines` → `capture_screenshot`。绝对不能先读取 data/ 目录下的缓存 JSON 文件而跳过 TV MCP。只有 TV MCP 重复不可用（3+失败）时才 fallback 到本地缓存数据。
- **TV 截图强制**：分析卡必须带 TV 截图（full 窗口，含右侧价格栏+底部 CVD），通过 TV MCP `capture_screenshot` 获取
- **频道路由**：BTC→话题386 / XAU→话题385 / 山寨+报告→话题416
- **加密 15m 主周期 · 黄金 5m 主周期 · 外汇/期货 15m 主 · 股票 1h 主**
- **全市场统一五层周期 D→4h→1h→15m→5m**，不可跳层。截图必须切到该品种的主执行周期截 full。
- 黄金五层：D（宏观结构）→4h（中期趋势）→1h（结构确认）→15m（过渡）→5m（主执行+截图）
- 加密五层：D→4h→1h→15m（主执行+截图）→5m
- **TV DMI 注入**：③量价分析下方嵌入 📺 TV DMI 行（等级·处理·背景·CVD·执行·风控）
- **推送告警方向标注**：首行必须标注方向（↑做多/↓做空/○等待/×禁做）+ 等级 + 现价 + VWAP，禁emoji。例：`↑做多 A 65,042 · VWAP 64,551` / `×禁做 X 65,057 · VWAP 64,551`。格式详 §③ 推送告警卡。

## ⑩ 完整范例（2026-06-22 最新美化优化版 · XAUUSD）

```
**① 品种：XAUUSD · OANDA**
**② 周期：**
5m 逼进VWAP — 亚盘急拉4220后持续回落，现价`4,180`紧贴周VWAP`4,181`
15m 偏空加速 — 三连阴+EMA9下穿21·CVD`-3,374`卖压扩大
1h 空头偏 — TV判空1h·位置从上方控制→下方控制
4h 反抽遇阻 — 月VWAP`4,295`压顶·VAH`4,216`确认强阻

**③ 现价：`4,180` · 高 `4,220.56` · 低 `4,136.57` · 日变动 +0.59%**
**④ 状态：C等待 · 偏空**
**⑤ 模型：VWAP反抽**
**⑥ 评分：6/13 · CVD加速卖压+TV判空1h+EMA下穿**
**⑦ 决策：VWAP`4,181`正在测试 — 等收盘确认 · 置信 2/5**
**⑧ 仓位：轻仓 · 最大风险 1.4U**
**⑨ 失效：`4,157`(DO) · 有效期 亚盘至欧盘开**
**⑩ 数据：B · 2源 · CVD`-3,374`A级**

**1. 环境**
亚盘开`4,149`→急拉`4,220`→回落`4,180`，现价贴VWAP`4,181`
沪银`-6%`·WTI`-2%`—商品空
📺 TV DMI: C等待 · 空1h · 下方控制 · CVD顺空确认
执行：多→破VWAP / 空→反抽VWAP不破 ← 偏空倾向

**2. 结构 — 逐周期分解**
4h：反抽VAH`4,216`受阻 — CVDSlope`-3,284`卖压加速
VAH`4,216`/VAL`4,160`/POC`4,188`/DO`4,157`
1h：TV空1h — EMA9`4,185`即将下穿21`4,188`死叉酝酿
5m：现价`4,180`贴VWAP`4,181`差`$1` — Last5bars连续走低

**3. 博弈**
多：VWAP`4,181`支撑+周五VAH`4,179`站稳→反抽`4,205`
空：CVD加速+TV空1h+商品普跌→破VWAP看`4,160`
三源一致：偏空 — TV空1h+下方控制+CVD顺空

**4. 操作 — 预案A · 反抽VWAP不破做空 ⚠三源偏空**

① 方向：空头
② 入场：`4,181` 现价区
   确认：5m收VWAP下 · CVD`-3,300`以上 · EMA9死叉21
③ 风控：1000x
   止损：`4,195` 14点
   止盈：`4,161` VAL (1:1.4)
   止盈：`4,138` 周五VAL (1:3.0)
④ 仓位：0.01手 · 风险 1.4U
⑤ 退出：
   失效：30min反拉站上`4,190`
   预案：反拉站稳→取消空单
⑥ 轨迹：VWAP测试 → 反抽不过破位 → 目标VAL/DO

**4. 操作 — 预案B · VWAP支撑反弹做多**

① 方向：多头
② 入场：`4,180` VWAP反弹确认
   确认：15m双底不破VWAP · CVD卖压衰减 · EMA9拐头
③ 风控：1000x
   止损：`4,170` 10点
   止盈：`4,205` +1σ (1:2.5)
   止盈：`4,216` VAH (1:3.6)
④ 仓位：0.01手 · 风险 1.0U
⑤ 退出：
   失效：VWAP收盘破`4,176`
   预案：破位→反向触发A
⑥ 轨迹：VWAP支撑 → 反弹破EMA9/21 → 目标+1σ/VAH

**5. 风控**
① 单笔：1-3U · 轻仓
② 日限：5U
③ 移损：+1.5R移保本
④ 止损：空`4,195`(14点) / 多`4,170`(10点)
⑤ 止盈：A→`4,161`·`4,138`/B→`4,205`·`4,216`
⑥ R:R：空1:1.4/3.0 · 多1:2.5/3.6
⑦ 心态：VWAP`4,181`最终战 — 收盘确认后入场
```

## 附注

此 skill **约束格式 + 提供完整模板范例**。数据采集、TV集成、模型判断由各对话自行处理。

任何会话加载此 skill 后出卡必须对上 ↑ 格式。格式不对 = 需要切回重写。

### Telegram RichMarkdown 交易卡首屏规则（2026-07-03新增）

正式交易卡/分析卡/审计卡只要包含表格，必须通过 Bot API 10.1 `sendRichMessage` + `RichMarkdown` 真表格发送；普通聊天回复中的 Markdown 表格会降级成项目符号，不满足棠溪要求。首屏必须按“北京时间标题 → 当前状态行 → 当前走势说明 → 核心结构位 → 直接结论”排版，后续用编号小标题 + 真表格。完整发送模板、首屏范例、降级坑点见 `references/telegram-rich-trading-card-layout-2026-07-03.md`。

**发送方式**（2026-07-08 实测确认）：
- ❌ `hermes send` / `hermes_cli.main send` → 只发 `sendMessage` 纯文本，不解析 RichMarkdown，表格显示为 `|` 竖线
- ✅ `scripts/telegram_reliable.py` 的 `send_telegram_reliable(parse_mode='RichMarkdown')` → 走 Bot API 10.1 `sendRichMessage` 渲染真表格，回执 `True rich_sent`
- Cron 的 no-agent 模式（stdout→hermes send）不能走 RichMarkdown，脚本需自行调用 `telegram_reliable.send_telegram_reliable()` 推送

**关键文件**：`D:/Hermes agent/scripts/telegram_reliable.py`，支持自动检测表格并路由到 `sendRichMessage`，失败时自动降级到普通 `sendMessage`。

#### 首屏美观规则（2026-07-04/07-06 棠溪纠正）

用户明确指出正式卡前置过程句“有点难看”，并在 2026-07-06 连续纠正交易卡样式：首屏不能是大段文字；普通 Markdown 表格在 Telegram 会降级成项目符号，不算真表格；标题和表格标题要加粗；价格前必须先写结构位；正文第一列必须标记侧重点；禁止互动式提问/投票，安禾必须基于多源验证直接裁决。后续用户确认 BTC 跟踪卡首屏“差不多这个的排版”：**中文时间 + BTC → 一、现在在哪 → 二、现在怎么做 → ①-⑥目录**。

**正式交易卡首屏优先级**：
1. 先发 MEDIA 全屏 TV 截图。
2. RichMarkdown 卡首屏用目录式交易员简报，不用长段落、不要流程日志。
3. 首屏必须先回答 `一、现在在哪：...` 与 `二、现在怎么做：...`，再列后续段落目录。
4. 正文段落固定按 `① 当前结构 / ② 多周期定位 / ③ 关键位矩阵 / ④ 多源验证 / ⑤ 执行方案 / ⑥ 最终裁决` 展开，每段下面接真表格。
5. 所有价格前加结构位：`POC 61,873`、`VAL 61,334`、`VAH 63,008`、`VWAP 62,487`、`DO 63,617`、`周日低 62,410`，禁止只写裸价格。
6. 下方表格第一列标侧重点：`【当前】/【脚下】/【防守】/【第一空点】/【主空区】/【失效】/【主动买卖】/【持仓】/【情绪】/【风控】`。
7. 用户不要互动问答。禁止写“你偏哪边 A/B/C”“请选择”“要不要我继续”等；直接给“有机会/陷阱/禁做/等哪里”的裁决。
8. 发送完成后的普通 assistant 回复只保留 `MEDIA` 截图、RichMarkdown回执和一句最终裁决；不要把正式表格卡再复制到普通回复里。

以后正式卡正文禁止出现工作流日志式开头，例如“开始完整驾驶舱 / TV五层完成 / 补外部源 / 准备截图”。这些只属于内部执行过程，不进入卡片。正式卡首屏必须像这样：

```markdown
## **2026年7月6日21：37 · BTC**

## **一、现在在哪：急跌后的支撑墙上方**
## **二、现在怎么做：不追空，等反抽**

## **① 当前结构**
## **② 多周期定位**
## **③ 关键位矩阵**
## **④ 多源验证**
## **⑤ 执行方案**
## **⑥ 最终裁决**
```

正文示例：

```markdown
## **① 当前结构**

| 侧重点 | 结构位 | 动作 |
|:---|:---:|:---|
| **【当前】POC争夺** | `61,873-61,911` | 不开新仓 |
| **【防守】VAL/M VWAP** | `61,334 / 61,305` | 跌破才续空 |
| **【主空区】周日低/W VWAP** | `62,410-62,470` | 失败做空 |
```

对话式普通回复可以简短；但正式 RichMarkdown 卡若有开头判断段，必须按上述“中文时间 + 一、二判断 + ①-⑥目录”结构，不要写成无标签长段落，也不要把首屏写成流程完成清单。

```markdown
## 2026年7月4日00：01 · BTC

○ 等待 · 现价 `61,922` · 主区间 `61,812-62,155`

BTC从 `62,400` 冲高后回落，当前贴近15m POC/VWAP上方震荡；1h结构仍偏多，但5m与15m执行层转弱，说明这里不是追多点。

关键位：VWAP `61,812` · POC `61,932` · VAL `61,576` · 压力 `62,155→62,400`

结论：先等 `61,812-61,576` 回踩守住再轻仓多；若15m收破 `61,540-61,480`，回踩多失效，转看 `61,229`。
```

发送完成后的普通 assistant 回复只保留 `MEDIA` 截图、RichMarkdown回执和一句最终裁决；不要把正式表格卡再复制到普通回复里。

## ⑪ 2026 联网社区融合增强

以下来自多渠道（Reddit ICT、Medium SMC策略、Bookmap CVD、LiquidityFinder黄金指南、Coinmonks KillZone）的 2026 社区共识验证，整合进分析卡逻辑。

### TV双指标直出推送卡（v1.0 · 2026-06-27 · Push场景替代格式）

当主指标(DMI)和副指标(Volume行动格)数据齐全时，推送卡可使用更简洁的6行格式替代v4.2格式：

```
↓做空 🔥A空 64,600 · VWAP 64,720 · 06-27 15:30
① 真实下跌 · 新空进场
② ▼新空进场 · ▼卖盘占优 · ▲放量
③ VAH 65,200 · VAL 63,800 · POC 64,550
④ A空 = 可做
```

**格式要点：**
- 首行：方向图标(↑↓○×) + 等级emoji + 等级 + 价格 + VWAP + 时间
- ①行：副指标结论（OI综合判定）
- ②行：OI + CVD + 量能 三项证据
- ③行：VAH/VAL/POC 关键位
- ④行：操作建议

渲染器：`scripts/render_tv_card.py`，字段映射：`tradingview-indicator-analysis` skill 的 `references/tv-dual-indicator-field-map.md`。

### 黄金 SMC 五步执行流

```
Step 1  标记关键位 — 未测试的 PDH/PDL、周高低点、VAH/VAL
Step 2  等流动性扫荡 — 价格扫掉止损后等突破或拒绝（15m 上确认）
Step 3  确认位移 — 强推力离开+FVG 形成才算有效 BOS
Step 4  FVG 入场 — 回填 FVG 50%→执行（3烛执行/低点执行/50% 执行）
Step 5  目标下个流动性池 — 反向 PDH/PDL、失衡区、OB
```

### CVD 社区共识（Bookmap · Reddit OrderFlow）

#### 背离（交易信号）
- **看多背离**：价格更低低点，CVD 更高低点 → 卖压衰减 → 潜在反转
- **看空背离**：价格更高高点，CVD 更低高点 → 买力衰竭 → 潜在反转
- *关键时间规则：背离必须在已知参考位（前高/前低/VWAP区/流动性区）出现，等反应确认后再操作*

#### 吸收（机构行为）
- CVD 在一个方向猛烈推进但价格几乎不走 → 巨大的被动玩家在吸收 → 价格将反转
- **VWAP 测试 + CVD 吸收**：价格触碰 VWAP 但 CVD 显示对侧未能击穿 → 潜在转向

#### 突破滤波器
- 价格离开 VWAP 但 CVD 不扩张 → 突破质量低
- **VWAP 收复 + CVD 向上** = 买方真正参与 → 高确认

### Kill Zone 黄金时刻表（EST 换算 Asia/北京时间 +8）

| 时段 | EST | 北京时间 | 特征 |
|------|-----|----------|------|
| **亚洲** | 7PM–3AM | 次日07:00–15:00 | 筑蓄积区，低波动，20-30pips 震荡 |
| **伦敦** | 2AM–5AM | 14:00–17:00 | 最高波动，亚洲区突破，50-100pips 波动 |
| **纽约** | 8AM–11AM | 20:00–23:00 | 伦敦-纽约重叠+数据驱动，方向加速/反转 |
| **伦敦收盘** | 10AM–12PM | 22:00–次日00:00 | 获利了结，日内趋势收尾 |

黄金执行：伦敦(14:00-17:00)最优 — 亚洲区高低点做突破回测入场

### 溢价/折价区（Premium/Discount）

- 任意区间画 50% 线 — **折价区(下方)只做多，溢价区(上方)只做空**
- 忽略此规则→即使在完美 OB/FVG 也会被止损

### 仓位管理 — 分数凯利（社区 2026 标准）

- **Kelly% = (胜率×平均R - (1-胜率)) / 平均R**
- 实际仓位 = Kelly% × 0.25~0.50（分数凯利，防过度杠杆）
- 默认仓位上限：本金 × 1%（棠溪：100U → 1U/笔）
- 连续亏损 ≥3 次 → 缩半仓，≥5 次 → 暂停

### ⑫ 多源情绪/Summary 表格格式（2026-07-01 棠溪确认·所有非完整卡场景强制）

用于 X 情绪分析、市场概况、Orion 雷达简报等**非完整交易卡**场景的纯表格格式。完整交易卡仍走 §② 骨架。此格式是 Telegram 推送 cron 的默认格式。

#### 核心规则

| 规则 | 说明 |
|:----|:-----|
| 表数量 | **恰好 3 张**（行情全景·多源交叉验证·关键位预案），不多不少 |
| 每表列数 | **≤3 列**（紧凑可用 4 列，但必须精简） |
| 表格格式 | **真 Markdown 管道表** `\| col \| col \| col \|`，不可用文字对齐冒充表格；Telegram RichMarkdown 下表格前不要单独放 `表1 · xxx` 标题行，否则会降级成文字 |
| 内容要求 | 每行必须有经 x_search / Binance / 链上 **验证过的具体数据**，不可空洞笼统 |
| 方向判断 | 表3 必须有明确的多/空/禁/等待判断，不可模棱两可 |
| 数据标注 | 表2 方向列固定用 🐻偏空 / 🐂看涨 / ⚖中性 / ⚡异动 / ❌陷阱 |
| 时间标注 | 首行：方向标注 + 时间（中文日期格式） |

#### 格式模板

```
↓ 方向标注 · BTC $58,472 · 2026年07月01日08：38

| 品种 | 现价 | 变动 |
|:----|:---:|:----:|
| BTC | `58,472` | −2.2% |
| ETH | `1,572` | −1.4% |
| SOL | `73.66` | −0.8% |

总市值 −1.77% · BTC占比 55.3%

| 来源 | 方向 | 验证数据 |
|:----|:---:|:---------|
| 恐惧贪婪 | 🐻极恐 | 11/100·历史底部区 |
| X·BTC短期 | 🐻偏空 | $58K支撑测试·ETF月流出$4.1B·13连撤 |
| X·链上 | 🐂积累 | 30天积累270K BTC·交易所余额新低 |
| X·衍生品 | 🐻空头蓄力 | 正费率+OI高位不降=空头加仓·爆仓$58-59K |
| Web·宏观 | 🐻承压 | 市值蒸发48%·FOMC偏鹰·7月预测$50.8K-$66.2K |
| Orion异动 | ⚡预警 | TAIKO桥修复反弹·ONG OI暴增陷阱 |

| 方向 | 条件 | 动作 | 目标 |
|:---:|:----|:----|:---:|
| ↓空 | BTC破`58,000`+放量 | 观望等`55K`以下抄底 | `55K`/`50K` |
| ↑多 | BTC`58K`双底+站回`59,500` | 轻仓多 | `60K`→`62K` |
| ⚠风控 | 鲸鱼日流入>$1B | 减仓观望 | 链上转抛售 |
| ×禁做 | F&G<10+ETF加速流 | 全清仓守现金 | — |
```

#### Telegram RichMarkdown 真表格专项

详见 `references/telegram-richmarkdown-tables.md`。核心规则：Telegram 手机端真表格必须走 Bot API 10.1 `sendRichMessage` + `rich_message.markdown`；表格前不能紧贴 standalone `表1 · xxx` 标题行，否则会降级成文字。发送器应自动剥离这类标题行，并用回执里的 `rich_message.blocks[type=table]` 验证。

#### 常见错误（棠溪多次纠正·必读）

| 错误 | 表现 | 正确做法 |
|:----|:-----|:---------|
| ❌假表格 | 文字用空格/破折号对齐，看起来像表但不是 Markdown 管道表 | 必须用 `\| col \| col \| col \|` |
| ❌表太多 | 输出 5-8 张表，信息过载 | 压缩到 3 张（行情/验证/预案） |
| ❌列太多 | 每表 5-6 列，行宽过大 | 控制在 ≤3 列，数据放「验证数据」列内 |
| ❌内容空洞 | 「偏空」「需关注」无具体数据 | 必须有具体数值（价格/百分比/流量） |
| ❌无判断 | 表3只有分析没有多空结论 | 每行必须有方向+动作+目标 |
| ❌来源模糊 | 不标注数据来源 | 每行注明 x_search/Binance/CG/F&G 等 |

### ⑬ Telegram 真表格铁律：RichMarkdown 文本表，禁止图片表

**2026-07-08 用户明确纠正（最高优先级，覆盖此前任何「图片化」指令）**：
- *"必须要纯 Markdown 管道表格，渲染 RichMarkdown 文本表"*
- *"不要照片的表格，我很讨厌照片的表格"*

→ **所有推 Telegram 的表格一律走 RichMarkdown 文本管道表。图片表 / HTML→PNG 卡 一律禁用。** 此前的「正式交易卡一律图片化」「表格优先渲染图片表」指令作废。

**发送通道（关键，决定能否渲染真表格）**：
- ❌ `hermes send` / `hermes_cli.main send` → 走 `sendMessage` 纯文本，`parse_mode=MarkdownV2`，**不渲染管道表**，退化成裸 `|` 竖线。
- ✅ `scripts/telegram_reliable.py` 的 `send_telegram_reliable(parse_mode='RichMarkdown')` → 走 Bot API 10.1 `sendRichMessage` 渲染真表格，回执 `true rich_sent`。
- ✅ 便捷封装 `push_tg_rich(target, text)` → 内部调 `send_telegram_reliable(parse_mode='RichMarkdown', retries=3)`，所有脚本直接 `from telegram_reliable import push_tg_rich` 调用。

**Cron 推送任务统一改造模式（2026-07-08 跑通，7个推TG任务全部适用）**：
1. 脚本内 `print(report)` 同时 `push_tg_rich("telegram:-1003733144325:846", report)`（report 为纯 Markdown 管道表字符串）。
2. 把该 cron 的 `Deliver` 改为 `local`（`hermes cron edit <id> --deliver local`），**避免 cron 再用 `hermes send` 的 MarkdownV2 通道发一遍退化版**，造成双重发送或裸 `|` 文本。
3. 表格前不要紧贴 standalone `表1 · xxx` 标题行，否则 RichMarkdown 可能降级成文字；让表格直接跟在空行后。
4. 验证：跑脚本看 stdout 表格 + TG 回执 `rich_sent`；用户侧确认渲染为真表格。

**决策精细度要求（2026-07-08 用户纠正「信息太简单，缺辅助决策」）**：推送报告不能只给标志/状态，必须带可辅助决策的具体维度：
- **交易类任务**（BTC关键位/Orion雷达）：现价 + 各关键位(POC/VWAP/VAH/VAL/DO)价位 + **相对现价的偏离%** + 波动区间/振幅 + 明确的多空动作建议；Orion 候选加 24h% 与 **仓位系数**(0×~1.0×) 建议。
- **情绪类任务**（X情绪）：恐惧贪婪值 + **分层解读**（极度恐惧·潜在抄底区 等）+ BTC/ETH占比 + 24h市值变化 + 活跃币种数 + **Orion共振候选表**(置信/1h/OI/费率联动)。
- **成功路径也必须推**：原 BTC关键位同步脚本成功时完全静默(只写JSON不推TG)是决策信息缺口，改为成功也发精细表。失败路径照常发诊断表。
- 字段优先用脚本已采集的数据，不凭空编造；缺数据用 `—` 占位。

**为什么 RichMarkdown 文本表足够（替代图片表的理由）**：
- 用户客户端确认 RichMarkdown 真表格渲染正常（"是的，真表格"），无需图片兜底。
- 图片表是像素固定、不可复制/不可检索的死图，用户明确厌恶。
- 文本表可被 LLM/脚本二次解析，符合棠溪数据驱动工作流。

**降级通道（仅当 sendRichMessage 返回失败且无本地脚本能力时）**：退化为纯文本告警，但**仍不得用图片表**。

### ⑭ 渲染器代码层铁律（2026-07-08 实战纠正 · 最高优先级）

卡片格式要求"真表格"不只约束 prompt/文案，**渲染器源码也必须产出真管道表**。2026-07-08 发现 `render_v96.py` / `render_tv_card.py` 的结构位前置用全角竖线 `｜`.join 拼成 `🔴VAL61,663｜⚖现61,830｜🟢支61,708` 单行伪表格，TG RichMarkdown 不渲染成表 → 用户一眼看出"分析模板不是真表格"。

**铁律**：
- 结构位前置（`【现在】`/`【结构】`）必须调用返回**真管道表**的函数：`_level_table()`（render_tv_card）/ `_structure_table()`（render_v96），格式 `| 结构 | 价格 | 距现价 |` + `|:---|:---:|---:|` + 数据行（上沿/现价/下沿三行带距现价%）。
- `【做法】`/`【依据】`/`【决策摘要】` 必须做成真管道表（`| 维度 | 内容 |` + `|:---|:---|`），**严禁**用全角 `｜` 拼接单行伪表格，也**严禁**把伪表格串塞进真表格单元格（会破坏整张表结构）。
- 永久禁止 `_level_strip` / `_structure_strip` 这类 `｜`.join 拼接函数；表格单元格内若需嵌多值，改用已清洗无 `｜` 的真实价字段（如 `entry`/`magnet`）。
- 全局搜 `｜` 时只允许"解析/防护"逻辑（拆串、转义），不允许"生成"逻辑。

### ⑮ 夜间静默门（2026-07-08 用户要求）

用户要求：**晚上 23:00–次日 08:00 不提示，但后台任务照常运行**。统一在 `telegram_reliable.push_tg_rich` 入口加时段 gate：

```python
now_h = datetime.now(TZ).hour
if now_h >= 23 or now_h < 8:
    return True, "silent_night"   # 不打 TG、不落盘 pending、cron 视为正常完成
```

- 所有 50+ 推 TG 的 no_agent 脚本都经 `push_tg_rich` → 一处 gate 全量生效，**无需逐个改脚本**。
- 返回 `(True, "silent_night")` 而非 `(False, ...)`：避免 cron 误判失败、避免 pending 堆积、避免静默时段刷屏。
- 后台计算（采集/刷新/验证）不受任何影响，仅在发送环节拦截。
- 时段边界：`now_h>=23 or now_h<8` 即 23:00–07:59 静默，08:00–22:59 推送。

### 银弹策略真实回测率（2026 社区验证）

社区共识回测胜率 50-65%（非宣传的 70-80%）：
- 40% 进场后先浮亏 → 必须给足 15m ATR 宽度空间
- 预期 1:2 R:R × 55% 胜率 → 期望值 +0.65R/笔
- 棠溪回测优先级已固定：VWAP反抽>POC拒绝>VAH回收>突破接受

### 扫荡后回测规则（Retest After Sweep）

**2026 社区确认最稳定入场模式**：
1. 价格扫掉一个流动性池（新高/新低甩掉追盘）
2. 立即反转回到原区间
3. 回测刚刚被扫的价位（原阻力变支撑 / 原支撑变阻力）
4. 在此回测位入场 → 目标下个流动性池 / 1:2-1:3 R:R

此规则融入操作段「确认」条件：`5m突破回测VAH不破`·`扫亚洲低后反弹回测原支撑`