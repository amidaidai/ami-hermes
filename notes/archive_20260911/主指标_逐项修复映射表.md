# 棠溪主指标逐项修复映射表

> 基于源码 `eb05e066077677c2.txt` (2859行, SHA: `1cd1d916957d6bc6848c82ccd30b64dbca601f3721aee54bcb9a3dbf434b96de`) 与审计报告 `棠溪双指标全面审计_2026年7月10日.md`
> **仅输出建议，不修改文件**。P0→P1→P2 顺序执行。

---

## 目录
1. [P0 级：生产文件分叉 / 编译层](#p0-级生产文件分叉--编译层)
2. [P1 级：核心逻辑缺陷 (OB/Breaker/HTF/CVD/评分/MCP/告警/锚定/亚洲CVD/日周池/扫线)](#p1-级核心逻辑缺陷)
3. [P2 级：工程质量 (无效输入/死代码/UI术语/锚定规则/HTF优先级括号)](#p2-级工程质量)

---

## P0 级：生产文件分叉 / 编译层

| 编号 | 问题 | 依赖顺序 | 操作建议 | 编译陷阱 |
|---|---|---|---|---|
| **P0-1** | 三份主指标文件 SHA 不一致 (`1cd1d91` vs `976ee88` vs `662c30f`) | **第 0 步** | 1) 以附件 `1cd1d916` 为唯一权威源  <br>2) `Desktop\SVP_v6_ready.pine` 覆盖写入该内容  <br>3) `Upload\SVP_v6.pine` / `Upload\SVP_v6_ready.pine` 同步覆盖  <br>4) TradingView 编辑器 “Save” 再 “Publish/Update” | 只改其中一份会导致 TV/MCP 读到旧逻辑；Pine 编译缓存需强制刷新 |
| **P0-编译** | 无编译阻断，但 `max_lines_count=500`、`max_boxes_count=120` 较紧 | P0-1 后 | 若后续新增 OB/Breaker/FVG 绘制对象，需同步调大 `max_*_count` 与 `max_polylines_count=100` | 超限会在运行时静默截断绘图，不报错 |

---

## P1 级：核心逻辑缺陷 (按依赖顺序排序)

> **依赖链**：P1-2 OB选K → P1-1 Breaker翻转 → P1-3 HTF OB非重绘 → P1-5 方向专属confirmScore → P1-6 MCP Executable → P1-4 CVD强度门控 → P1-11 告警门控 → P1-8 贵金属亚洲CVD → P1-9 ICT开关不误删日周池 → P1-10 活跃会话开盘线 → P1-7 资金费率改名

---

### P1-2：OB 选错蜡烛 (影响 Breaker/评分/MCP 编码)
| 项目 | 内容 |
|---|---|
| **源码位置** | L2198-L2209 (看涨OB) / L2210-L2221 (看跌OB) / L741-L756 (`f_htf_ob`) |
| **问题** | 找到最后一根阴线 `obOffset` 后，**取 `obOffset+1` 更老的阳线** 作为看涨 OB；看跌同理。导致区间整体偏移 1-2 根 K。 |
| **建议替换 (看涨 OB)** | ```pine\nif obBullDetected\n    int obOffset = na\n    for k = 1 to 10\n        if close[k] < open[k]          // 最后一根阴线\n            obOffset := k\n            break\n    // 【修】直接用这根阴线作为 OB，不再 +1\n    if not na(obOffset)\n        float obTop = high[obOffset]\n        float obBot = low[obOffset]\n        // 验证：该阴线前一根必须是阳线 (位移确认)\n        if obOffset + 1 <= 10 and close[obOffset + 1] > open[obOffset + 1]\n            box nb = ...\n            array.push(obList, OBZone.new(nb, olb, obTop, obBot, true, false, false, false, bar_index - obOffset))\n``` |
| **建议替换 (看跌 OB)** | 同上，**取最后一根阳线本身**，再验证前一根为阴线。 |
| **HTF OB 同步修正** | `f_htf_ob()` L741-L756 逻辑同理：`obTop := high[off]` / `obBot := low[off]`，**不要 `off+1`**。 |
| **依赖** | 必须先于 P1-1 (Breaker 翻转逻辑读取 `ob.isBull`)，否则 Breaker 方向仍会用错向 OB。 |
| **编译陷阱** | `obOffset` 可能为 `na`，需 `not na(obOffset)` 保护；数组下标 `obOffset+1` 需 `<=10` 判断已存在。 |

---

### P1-1：Breaker 方向反了 (影响评分/MCP/告警/行动格)
| 项目 | 内容 |
|---|---|
| **源码位置** | L2222-L2236 (Breaker 翻转) / L2290-L2298 (`inBullBreaker`/`inBearBreaker` 写入) / L2357/L2429 (评分读取) / L2786 (`mcpObCode`) |
| **问题** | 看涨 OB 跌破后仅置 `isBreaker=true`，**不翻转 `isBull→false`**；评分仍把 `inBullBreaker` 当作做多汇合。 |
| **建议替换 (L2227-L2235 区块)** | ```pine\nif SHOW_BREAKER and array.size(obList) > 0\n    int oiBrk = array.size(obList) - 1\n    while oiBrk >= 0\n        OBZone ob = array.get(obList, oiBrk)\n        if not ob.mitigated and not ob.isBreaker\n            bool broken = ob.isBull ? close < ob.bot : close > ob.top\n            if broken and barstate.isconfirmed\n                ob.isBreaker := true\n                // 【修】翻转有效方向\n                ob.isBull := not ob.isBull\n                // 颜色/标签同步\n                if not na(ob.bx)\n                    box.set_bgcolor(ob.bx, color.new(ob.isBull ? BRK_BULL_COL : BRK_BEAR_COL, OB_FILL_TRANSP))\n                    box.set_border_color(ob.bx, color.new(ob.isBull ? BRK_BULL_COL : BRK_BEAR_COL, 40))\n                if not na(ob.lb)\n                    label.set_text(ob.lb, ob.isBull ? \"BRK↑\" : \"BRK↓\")\n                    label.set_color(ob.lb, color.new(ob.isBull ? BRK_BULL_COL : BRK_BEAR_COL, 20))\n        oiBrk -= 1\n``` |
| **联动修改** | 1. L2290-L2298 `inBullBreaker/inBearBreaker` 直接读取 `ob.isBull` (已翻转) → 无需改动<br>2. L2357/L2429 评分 `inBullBreaker/inBearBreaker` 自动正确<br>3. L2786 `mcpObCode := inBullOB or inBullBreaker ? 1 : inBearOB or inBearBreaker ? -1 : 0` 自动正确<br>4. L2282-L2289 标签显示 `ob.isBreaker ? (ob.isBull ? \"BRK↑\" : \"BRK↓\") : ...` 已正确 |
| **依赖** | 依赖 P1-2 (OB 本身方向正确)；必须早于 P1-5/P1-6。 |
| **编译陷阱** | `ob.isBull := not ob.isBull` 需 `OBZone` 类型字段可变 (已是 `var` 数组元素，可直接赋值)。 |

---

### P1-3：HTF OB 存在前瞻/重绘
| 项目 | 内容 |
|---|---|
| **源码位置** | L734-L735 `f_htf_ob()` 直接读取当前 HTF `close` / L2016 `request.security(..., lookahead=barmerge.lookahead_on)` |
| **问题** | 低周期实时 bar 能看到 HTF 当前未收盘 `close`，导致 HTF OB/BOS 提前出现、历史回测不重绘但实时漂移。 |
| **建议替换** | 1. `f_htf_ob()` 内部**仅使用已收盘 HTF 柱** (`close[1]`, `high[1]`, `low[1]` 等)；<br>2. 调用处 `request.security(..., lookahead=barmerge.lookahead_on)` 保留，**整体结果取 `[1]` 偏移**：<br>```pine\n[obHBull, obHBear, obHTop, obHBot] = request.security(syminfo.tickerid, fvgHtfRes, f_htf_ob(), lookahead=barmerge.lookahead_on, ignore_invalid_symbol=true)[1]\n```<br>3. `f_htf_fvg()` 同理已用 `barstate.isconfirmed` 与 `[1]` 偏移 (L2018-L2019)，**保持一致**。 |
| **依赖** | 独立，但建议在 P1-2 之后，以便 HTF OB 方向也正确。 |
| **编译陷阱** | `request.security(...)[1]` 返回元组需 Pine v6 支持 (已支持)；若 HTF 与当前周期相同会报错，`fvgHtfValid` 已含 `timeframe.in_seconds(fvgHtfRaw) > curTfSec` 守卫。 |

---

### P1-5：`confirmScore` 可被反向条件抬高且漏 FVG
| 项目 | 内容 |
|---|---|
| **源码位置** | L2439 `confirmScore` 计算 |
| **问题** | 1. 用 `bull or bear` 的 CVD/扫线/接受条件**统一加分**，不按当前计划方向<br>2. 完全无 FVG 项，但行动格显示 `FVG✓`，人工与评分不一致<br>3. 双边同时成立时无冲突闸门 |
| **建议替换** | ```pine\n// 先确定唯一计划方向\nbool planLong  = activeLongPlan\nbool planShort = activeShortPlan\n\nint confirmScore = 0\nif planLong\n    confirmScore += cvdBullConfirmQualified ? 2 : 0\n    confirmScore += sweepOrAcceptLong ? 2 : 0\n    confirmScore += acceptanceBullOk ? 1 : 0\n    confirmScore += (inBullOB or inBullBreaker) ? 1 : 0\n    confirmScore += inBullObHtf ? 1 : 0\n    confirmScore += inBullFvg ? 1 : 0                // 【新增】本级 FVG\n    confirmScore += inBullFvgHtf ? 1 : 0             // 【新增】HTF FVG\nelse if planShort\n    confirmScore += cvdBearConfirmQualified ? 2 : 0\n    confirmScore += sweepOrAcceptShort ? 2 : 0\n    confirmScore += acceptanceBearOk ? 1 : 0\n    confirmScore += (inBearOB or inBearBreaker) ? 1 : 0\n    confirmScore += inBearObHtf ? 1 : 0\n    confirmScore += inBearFvg ? 1 : 0\n    confirmScore += inBearFvgHtf ? 1 : 0\n// 【冲突闸门】双向同时成立直接置 0\nif planLong and planShort\n    confirmScore := 0\n``` |
| **依赖** | 依赖 P1-1/P1-2 (OB/Breaker 方向正确) 与 P1-3 (HTF FVG 非重绘)。 |
| **编译陷阱** | `inBullFvg`/`inBearFvg`/`inBullFvgHtf`/`inBearFvgHtf` 已在 L2080-L2151 计算好，直接可用。 |

---

### P1-6：MCP 与行动格仍可能漂移 (Executable Code 缺失)
| 项目 | 内容 |
|---|---|
| **源码位置** | L2773 先判 `activeLongPlan/activeShortPlan` 再判 `setupX` / L2522 `executablePlan` 定义 / L2773 `plot(activeLongPlan?1:activeShortPlan?-1:setupX?9:0, ...)` |
| **问题** | `setupX`(风险态) 仍导出 Target；BTC 实测 Side=9 但 Target 有值；XAU Side=0 仍有 Target。 |
| **建议替换** | 1. **新增显式 `executableCode` 变量** (供 MCP/外部脚本强检)：<br>```pine\nint executableCode = setupX ? 9 : executablePlan ? (activeLongPlan ? 1 : -1) : 0\n```<br>2. **MCP Side Code 改用 `executableCode`**：<br>```pine\nplot(executableCode, \"MCP Side Code\", display=display.data_window)\n```<br>3. **非可执行时 Entry/Stop/Target 全部 `na`**：<br>```pine\nfloat replayPlanPrice = executablePlan ? candidatePlanPrice : na\nfloat replayInvalidPrice = executablePlan ? candidateInvalidPrice : na\nfloat magnetTargetPrice = executablePlan ? magnetTargetPrice : na  // 已在 L2506 计算，需守卫\n```<br>4. 行动格 `panelEntryVal/panelStopVal/panelTgtVal` 已在 L2713-L2719 受 `rrHardBlock`/`executablePlan` 守卫，**保持**。 |
| **依赖** | 依赖 P1-5 (`confirmScore` 方向化) 与 P1-1/P1-2 (OB/Breaker 方向)。 |
| **编译陷阱** | `magnetTargetPrice` 在 L2506 已计算，L2511 `magnetTargetText` 也用它；需在计算处加 `executablePlan ? ... : na`，或改写 L2511 为条件表达式。 |

---

### P1-4：CVD 背离强度门控是死代码
| 项目 | 内容 |
|---|---|
| **源码位置** | L817-L818 `cvdDivSwingOk` / L828-L829 背离判定 / L1824-L1825 仅加质量/关键位 |
| **问题** | `cvdDivSwingOk` 全文仅出现 1 次，**未接入背离判定**；背离缺 1.5 ATR 幅度门槛与 CVD 枢轴斜率过滤。 |
| **建议替换** | 1. **计算 CVD 枢轴间斜率** (L819-L827 后追加)：<br>```pine\nfloat cvdBearDivSlope = cvdBearDivRaw ? (_cp - _cpp) / (_pl * 2 + 1) : na\nfloat cvdBullDivSlope = cvdBullDivRaw ? (_cl - _clp) / (_pl * 2 + 1) : na\n```<br>2. **背离合格标志**：<br>```pine\nbool cvdBearDivQualified = cvdBearDivRaw and cvdDivSwingOk and cvdBearDivSlope < -0.01\nbool cvdBullDivQualified = cvdBullDivRaw and cvdDivSwingOk and cvdBullDivSlope > 0.01\n```<br>3. **替换 L1824-L1825 及后续所有 `cvdBearDiv/cvdBullDiv` 使用处** 为 `Qualified` 版本。 |
| **依赖** | 独立，但 P1-5 `confirmScore` 已引用 `Qualified` 版，需同步。 |
| **编译陷阱** | `_pl` 等变量在函数内作用域；需将斜率计算移到同一作用域或用 `var` 保存。 |

---

### P1-11：扫线 + CVD 告警绕过质量门控
| 项目 | 内容 |
|---|---|
| **源码位置** | L2814-L2815 `alertSweepLowRepair/alertSweepHighReject` 使用 `cvdAbsorbBuy/cvdDistributeSell` (原始) 而非 `Qualified` 版 |
| **建议替换** | ```pine\nbool alertSweepLowRepair = ENABLE_PRO_ALERTS and alertConfirmOk and sweptLowReclaimed and (cvdBullDivQualified or cvdAbsorbBuyQualified)\nbool alertSweepHighReject = ENABLE_PRO_ALERTS and alertConfirmOk and sweptHighRejected and (cvdBearDivQualified or cvdDistributeSellQualified)\n``` |
| **依赖** | 依赖 P1-4 (`Qualified` 标志就绪)。 |
| **编译陷阱** | 无。 |

---

### P1-8：贵金属/外汇仍启用亚洲 CVD
| 项目 | 内容 |
|---|---|
| **源码位置** | L931 `bool cvdUseAsiaChannel = not MARKET_ADAPTIVE_ENGINE or marketCrypto or marketMetal or marketForex` |
| **问题** | XAU/FX 亚洲时段 CVD 噪声大，宜默认关闭。 |
| **建议替换** | ```pine\nbool cvdUseAsiaChannel = not MARKET_ADAPTIVE_ENGINE or marketCrypto or (MARKET_ADAPTIVE_ENGINE and (marketMetal or marketForex) and input.bool(false, \"贵金属/外汇启用亚洲CVD\"))\n```<br>或更简洁：默认仅 `marketCrypto` 启用，金/汇需显式开关。 |
| **依赖** | 独立。 |
| **编译陷阱** | 新增 `input.bool` 需放在 `CVD_SESSION_GROUP` 组内。 |

---

### P1-9：ICT 总开关误删日周池
| 项目 | 内容 |
|---|---|
| **源码位置** | L1515-L1525 `if barstate.islast and not SHOW_ICT_LEVELS` 删除整个 `levels[]`，其中含独立前日/前周池 |
| **建议替换** | 1. 将日/周池从 `levels[]` **分离**，用独立数组 `var array<ICTLevel> dayWeekLevels` 维护<br>2. `SHOW_ICT_LEVELS` 仅控制会话高低线与 FVG/OB/LV 标签合并<br>3. `SHOW_DAY_LIQUIDITY`/`SHOW_WEEK_LIQUIDITY` 分别控制各自池的绘制与清理 |
| **依赖** | 涉及 L1077-L1120 `ICTLevel` 类型与 `levels` 数组贯穿全文，**改动面大**，建议在 P1-1/P1-2 之后、P2 前完成。 |
| **编译陷阱** | 所有 `array.push(levels, ...)` / `f_remove_level_from_array` / 遍历 `levels` 的位置需同步拆分；建议新增 `f_push_dayweek_level()` / `f_clear_dayweek_levels()` 封装。 |

---

### P1-10：“活跃会话仅显示开盘价”没有真正开盘线
| 项目 | 内容 |
|---|---|
| **源码位置** | L1231-L1243 / L1281/L1288 |
| **问题** | 仅把活跃高低线设透明、清空标签，**未创建会话开盘价线**。 |
| **建议替换** | 在 `manageSession` 内 `justStarted` 分支新增：<br>```pine\nif justStarted\n    st.sOpen := open\n    // 【新】绘制会话开盘价虚线\n    if allowDraw and st.startTime > cutoffTime\n        st.lOpen := line.new(bar_index, st.sOpen, bar_index + 1, st.sOpen, color=color.new(sessionColor, 0), width=ICT_LINE_WIDTH, style=line.style_dashed)\n```<br>会话结束时 (`!isSession and isSession[1]`) 将 `st.lOpen` 设为实线并延伸。 |
| **依赖** | 独立。 |
| **编译陷阱** | `SessionState` 类型需新增 `line lOpen` 字段；`f_delete_ict_level` 需同步清理。 |

---

### P1-7：“资金费率”实际是永续溢价/贴水
| 项目 | 内容 |
|---|---|
| **源码位置** | L1020-L1025 `fundingText` 计算与输出 |
| **问题** | 仅算 `(perp-spot)/spot`，非真实资金费率。 |
| **建议替换** | 1. 重命名输入组 `FUNDING_GROUP` → `PREMIUM_GROUP`<br>2. 所有 `fundingText`/`资金` 改为 `永续溢价/贴水` / `Premium/Discount`<br>3. 如需真实资金费率，需 `request.security(\"BINANCE:BTCUSDT_PERP\", ..., funding_rate)` 等专用数据源 (TV 部分品种支持)。 |
| **依赖** | 独立。 |
| **编译陷阱** | 字符串替换全文搜索 `资金`、`funding`，注意变量名 `fundingText` 仅用于显示。 |

---

## P2 级：工程质量 (可并行处理)

| 编号 | 问题 | 源码证据 | 建议动作 | 编译陷阱 |
|---|---|---|---|---|
| **P2-1** | 18 个无效输入控件 | `VWAP_COLOR`、VWAP Band1/2 颜色、EMA 4组颜色/线宽、4组云颜色、`MACRO_COLOR`、`SB_COLOR`、`BOS_COL` 仅声明未消费 | 删除或接入对应 `plot`/`line`/`fill` | 删除前确认无 `input.color` 被其他变量引用 |
| **P2-2** | EMA 单线开关功能性失效 | L910-L913 `plot(..., color=color.new(..., 100))` 固定 100% 透明 | 改为 `SHOW_EMA_1 ? color.new(EMA_1_COLOR, 0) : na` 等 | 需同步修改 `display=display.all - display.price_scale` 保留 |
| **P2-3** | ≥8 个死函数/死变量 | `f_rebuild_polyline` L351, `f_near` L365, `f_dist_text` L367, `f_is_xag_pair` L963, `f_level_short` L1677, `f_score_text` L1922, `f_price_text` L1924, `f_strength_text` L1928 + 旧行动叙事变量 | 删除未被调用者 | 全文搜索调用引用后再删 |
| **P2-4** | HTF 开关优先级缺括号 | L2013 `fvgHtfValid = (SHOW_HTF_FVG and SHOW_FVG) or (SHOW_HTF_OB and SHOW_OB) and timeframe.in_seconds(fvgHtfRaw) > curTfSec` | 加括号：`((SHOW_HTF_FVG and SHOW_FVG) or (SHOW_HTF_OB and SHOW_OB)) and ...` | 逻辑优先级改变可能影响自动模式，测试 HTF FVG/OB 开关组合 |
| **P2-5** | 多市场锚定偏离棠溪规则 | L629 贵金属/股票 SVP 固定 D；L706 金/汇/股 4h 以上只到 W | 统一执行 `<1h=D、1h至<4h=W、≥4h=M` | 涉及 `targetProfileTF` 与 `autoProfileByMarket`，需全周期回测 |
| **P2-6** | UI 术语不符合棠溪口径 | 行动格 `扫2/5` → `已扫2/剩5`；图区 `FVG/OB/BRK/LV` → `缺口/订单块/破坏块/真空` | 字符串常量批量替换 | 含 `label.set_text`、`panelCheckText`、`actionIctText` 等多处 |
| **P2-7** | 截止时间仍用 K 线开盘时间 | L1151 `f_cutoff_ms` 用 `time` 而非 `timenow` | `int base = timenow - (lookbackDays * 86400 * 1000)` | 实时图最后一根 K 可能偏移 1 根，回测用 `time` 更稳，可加 `barstate.isrealtime` 分支 |
| **P2-8** | 副指标配额注释错误 / 死代码 / 非加密 Data Window 仍有方向值 | 副指标报告中已列出，主指标无需改 | — | — |

---

## 执行检查清单 (按顺序)

```
[ ] P0-1  统一三份文件 SHA
[ ] P1-2  OB 选 K 修正 (L2198/L2210/L741)
[ ] P1-1  Breaker 翻转 isBull (L2227)
[ ] P1-3  HTF OB 非重绘 (L734/L2016)
[ ] P1-4  CVD 背离 Qualified 门控 (L817/L828/L1824)
[ ] P1-5  confirmScore 方向专属 + FVG 分 (L2439)
[ ] P1-6  MCP executableCode + 非可执行 NaN (L2522/L2773)
[ ] P1-11 告警用 Qualified 版 (L2814)
[ ] P1-8  贵金属/外汇亚洲 CVD 默认关 (L931)
[ ] P1-9  日周池拆分独立数组 (L1515)
[ ] P1-10 会话开盘价线 (L1231)
[ ] P1-7  资金费率→永续溢价 (L1020)
[ ] P2-1  删除 18 无效输入
[ ] P2-2  恢复 EMA 单线开关
[ ] P2-3  清理死函数/变量
[ ] P2-4  HTF 开关加括号
[ ] P2-5  统一锚定规则
[ ] P2-6  UI 术语批量替换
[ ] P2-7  f_cutoff_ms 用 timenow
```

---

## 关键变量/类型变更影响表

| 变量/类型 | 修改涉及行范围 | 下游依赖 |
|---|---|---|
| `OBZone.isBull` (翻转) | 2227-2235, 2290-2298, 2357, 2429, 2786 | 评分、MCP OB Code、行动格 OB✓ |
| `OBZone.isBreaker` | 同上 | 标签显示、Breaker 计数 |
| `cvdBullDivQualified` / `cvdBearDivQualified` | 817-830, 1824-1825, 1959-1971, 2439, 2534-2535, 2814 | 评分、反转分、告警、行动格 CVD✓ |
| `executableCode` (新增) | 2522, 2773 | MCP Side Code、外部自动化脚本 |
| `levels[]` → `levels[]` + `dayWeekLevels[]` | 1077-1120, 1418-1426, 1515-1525, 1189-1202 | ICT 标签合并、扫线计数、磁吸评分 |
| `SessionState.lOpen` (新增) | 1090-1095, 1203-1245, 1272-1289 | 会话开盘线绘制/清理 |

---

## 编译/运行验证矩阵

| 阶段 | 命令/动作 | 通过标准 |
|---|---|---|
| 静态编译 | TradingView Pine 编辑器 “Save” | 0 Error, 0 Warning |
| 请求数审计 | 统计 `request.security/security_lower_tf` | ≤ 40 (默认方案) |
| 绘图对象上限 | `max_lines_count=500` 等 | 运行时无 "Too many drawings" 红框 |
| Data Window 回归 | BTCUSDT.P 15m / XAUUSD 15m | 12 项完整、`MCP Side Code` 仅 1/-1/9、`Executable` 与行动格一致 |
| 实盘冒烟 | 连续 2 小时 15m 图挂载 | 无重绘跳变、告警仅收盘触发、XAU 亚洲 CVD 不显示 |

---

## 附录：核心函数签名速查

```pine
// OBZone (L2034)
type OBZone
    box bx
    label lb
    float top, bot
    bool isBull, isBreaker, mitigated, htfConf
    int bornBar

// ICTLevel (L1077)
type ICTLevel
    line ln
    label lb
    float price
    bool isHigh, swept, isActive, isHidden
    int createdAt, priority
    string name
    color col

// VolumeProfileEngine (L383)
type VolumeProfileEngine
    float[] hPrices, lPrices, volumes
    float minProfilePrice, maxProfilePrice, priceStepSize
    polyline profileHistLower, profileHistUpper, valueAreaHistLower, valueAreaHistUpper
    line pocLevelLine, vahLevelLine, valLevelLine
    label pocLevelLabel, vahLevelLabel, valLevelLabel
    linefill vaFill
    float lastPocPrice, lastVahPrice, lastValPrice
```

---

> **提醒**：本表为“只读建议”，执行修改前请先在本地复刻源码跑一遍完整编译与 Data Window 回归，确认基线无误后再按顺序落笔。每完成一个 P1 项，建议立即 `Save` + `Add to Chart` 复核，避免链式报错累积。