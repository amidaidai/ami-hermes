# 棠溪分析卡 · 主模板 v6.9.3

定位：高质量精简正文 + 操作段详版。用于加密货币、贵金属、外汇、股票、期权的统一分析卡与监控策略源头。

权威规则：
① 每次出卡或改模板前必须先读取本文件。
② 正文只给人读，不出现任何本地结构化追踪键名或机器枚举。
③ 头部10段 + 正文五段完整；环境、结构、博弈、风控精简，操作详细。
④ 无表格、无引用块、无竖线、无装饰图标；纯纵向圈号与 `—` 解读。
⑤ 价格一律反引号；状态只用 A做多、A做空、B等待、X禁做。
⑥ R:R 底线 1:2；数据、事件、执行距离、日损、连亏任一硬闸不过则降级或禁做。
⑦ 每张完整卡末尾必须带 MEDIA 截图；BTC 优先 15m full + CVD，XAU 优先 5m full + CVD。
⑧ 真实成交必须走成交记录、成交复盘、策略治理闭环；样本少于 `20` 笔不升权。

社区对标依据：
① Freqtrade：信号计算向量化，入场、止损、仓位、杠杆、订单超时用回调分层；重计算不要放在高频回调。
② QuantConnect：回测与实盘必须建模手续费、滑点、成交、保证金、期权定价、容量限制。
③ NautilusTrader：数据、策略、执行、组合、回测引擎分层；多品种数据先统一排序与时间口径。
④ ICT/SMC/订单流社区：Liquidity Sweep 必须锚定关键位，并用 CVD背离/吸收 + Displacement 确认；孤立订单流不计分。
⑤ 黄金外汇社区：London/NY Kill Zone 是过滤器，常见序列为 Asia筑范围 → London扫荡 → NY确认。
⑥ 期权社区：方向判断之外必须写 Delta、Gamma、Theta、Vega、IV、到期日与最大亏损。

---

## 头部10段

```
**◷ {YYYY-MM-DD HH:MM} CST**
**① 品种：{SYMBOL}.P:{交易所}**
**② 周期：**
**5m {状态} — {原因}**
**15m {状态} — {原因}**
**1h {状态} — {原因}**
**4h {状态} — {原因}**
**③ 现价：`{price}` · 高 `{high}` · 低 `{low}` · 日变动 `{change}%`**
**④ 状态：{A做多/A做空/B等待/X禁做}**
**⑤ 模型：{VWAP反抽/VAH回收/VAL回收/POC拒绝/扫流动性回收/突破接受/无}**
**⑥ 评分：{n}/13 — {主要扣分}**
**⑦ 决策：{动作} · 置信 {n}/5 — {一句话理由}**
**⑧ 仓位：{正常/半仓/轻仓/禁止} · 最大风险 `{risk}U`**
**⑨ 失效：{价格或条件} · 有效期 {expiry}**
**⑩ 数据：{A/B/C} · {来源数}源 · {订单流/宏观/希腊值状态}**
```

头部字段规则：
① 品种要带交易所或经纪商，例：BTCUSDT.P:BINANCE、XAUUSD.P:EXNESS、EURUSD.P:OANDA、AAPL.P:NASDAQ、AAPL250117C.P:OPRA。
② 周期必须逐行写 5m、15m、1h、4h；每行只写状态与原因。
③ 模型只允许六类执行模型；不归属六类时只能 B等待 或 X禁做。
④ 评分用 13 分制：结构、周期、订单流/量价、衍生/宏观/希腊值、催化、风控、情绪。
⑤ 置信上限：结构与订单流分裂时最高 `3/5`；数据 C 级最高 B等待。

---

## 正文五段

### 1. 环境

```
**一、环境**
① 数据：{A/B/C} · {来源数}源 — 主源/校验源一致性 `{spread}%`
② 市场：{资产类核心因子} — 加密看 Funding/OI/Taker，黄金外汇看 DXY/美债/事件，股票看指数/板块/财报，期权看 IV/希腊值
③ 主动成交：{CVD/Taker/成交量/盘口} — 只在关键位附近计入
④ 流动性：上 `{上方}` · 下 `{下方}` — 止损池/挂单墙/前高低/价值区磁吸
⑤ 催化：{事件} · 等级{AA/A/B/C/D} — 时间、方向、是否禁做
⑥ 情绪：{X/社区/恐慌贪婪/新闻} — 只验证结构，不覆盖结构
⑦ 数据缺口：{缺失项/无} — 缺失项不加分
⑧ 风控环境：{允许/降级/禁止} — 数据、事件、流动性、账户状态综合
```

### 2. 结构

```
**二、结构**
① 4h背景：{趋势/震荡/反转} — 高周期方向与失效线 `{price}`
② 1h结构：阻 `{res}` · 支 `{sup}` — 接受/拒绝状态
③ 15m执行：触发 `{trigger}` · 禁追 `{no_chase}` — 必须收线确认
④ 5m触发：扫荡 `{sweep}` · CVD/量价 `{confirm}` · Displacement `{strength}` — 触发质量
⑤ 价值区：VAH `{vah}` · POC `{poc}` · VAL `{val}` · Naked POC {有/无}
⑥ 三线：失效 `{invalid}` · 执行 `{trigger}` · 禁追 `{no_chase}` — 错了在哪里认错
```

### 3. 博弈

```
**三、博弈**
① 结构裁决：{偏多/偏空/震荡} — 4h/1h 是否同向
② 引擎裁决：做空 {short_conf} vs 做多 {long_conf} — {共振/分裂}
③ 订单流裁决：扫荡 {已扫/待扫/无} · CVD/成交 {背离/吸收/顺向/冲突} · Displacement {强/弱}
④ 资产专属：{加密现货vs永续/黄金Kill Zone/外汇美元腿/股票板块/期权希腊值} — {结论}
⑤ 社区与情绪：{方向} · 强度{强/中/弱} — 只作验证或反向拥挤提醒
⑥ 三源裁决：结构{方向} + 引擎{方向} + 订单流{方向} — {共振/分裂}
⑦ 分歧处理：共振才允许预案；分裂则 B等待；硬闸不过则 X禁做
```

### 4. 操作

操作段必须最详细。默认预案A/B双轨；三源一致方向对应预案标 `⚠优先`。

```
**四、操作**

—— 预案A · {方向}（{触发条件} · {优先/备选}）——
① 方向：{多头/空头/观望}
② 入场：`{price}` · {限价/市价/等待触发}
   触发：{5m/15m收线条件 + 关键位 + 模型条件}
   确认：{结构确认} — {订单流/量价确认} — {资产专属确认}
③ 风控：{账户/交易所/杠杆/保证金规则}
   止损：`{price}` — {结构理由，不因止盈不足而放宽}
   止盈：`{price}` — {距离与逻辑}（1:{rr}）
   止盈：`{price}` — {距离与逻辑}（1:{rr}）
④ 仓位：`{qty}` {单位} · 风险 `{risk_usd}U` · 名义 `{notional}`
⑤ 失效：{价格/收线/事件条件} — 触发后取消本预案
⑥ 复查：入场后 {周期×根数} — 检查结构、订单流、移损、事件风险
⑦ 轨迹：{前态} → {触发} → {入场} → {复查} → {移损/止盈/失效}

—— 预案B · {反向或备选方向}（{备选原因}）——
① 方向：{多头/空头/观望}
② 入场：`{price}` · {限价/市价/等待触发}
   触发：{反向确认条件}
   确认：{结构失败/回收/接受} — {订单流/量价确认} — {资产专属确认}
③ 风控：{账户/交易所/杠杆/保证金规则}
   止损：`{price}` — {结构理由}
   止盈：`{price}` — {距离与逻辑}（1:{rr}）
   止盈：`{price}` — {距离与逻辑}（1:{rr}）
④ 仓位：`{qty}` {单位} · 风险 `{risk_usd}U` · 名义 `{notional}`
⑤ 失效：{条件} — 触发后取消本预案
⑥ 复查：入场后 {周期×根数} — 检查是否转强/转弱与是否移损
⑦ 轨迹：{前态} → {反向确认} → {入场} → {复查} → {移损/止盈/失效}
```

资产专属操作规则：
① 加密货币：必须写交易所、杠杆、Funding/OI/Taker/CVD、现货vs永续分化；BTC/ETH 默认 Binance 100x，其他加密默认 20x；突破接受必须无 CVD 背离。
② 贵金属：必须写经纪商、1000x 仅作账户上限、London/NY Kill Zone、DXY、美债、数据前后禁做；XAU 止损以美元距离和结构点位双写。
③ 外汇：必须写货币双腿、美元指数或交叉盘确认、点数、手数、隔夜利息、重大央行数据；不使用加密 Funding/OI。
④ 股票：必须写方向、股数、板块/指数共振、财报/停牌/流动性、盘前盘后风险；无杠杆或低杠杆优先。
⑤ 期权：必须写 Call/Put、到期日、行权价、权利金、Delta/Gamma/Theta/Vega、IV分位、最大亏损、时间衰减；方向对但 IV/Theta 不合格也只能 B等待。

### 5. 风控

```
**五、风控**
① 单笔：风险 `{risk_usd}U` — `10U` 是上限，不是默认
② 日限：已用 `{used}` / `{max_daily}U` — 达线停止新仓
③ R:R：{rr} — 低于 1:2 直接 X禁做
④ 止损：`{price}` — 结构止损优先，禁止为凑止盈放宽
⑤ 移损：+1R 移至保本 — +2R 保护利润
⑥ 数据闸门：{通过/降级/禁做} — 数据等级与缺口
⑦ 事件闸门：{通过/禁做} — 重大事件前后 {n} 分钟
⑧ 执行闸门：{通过/等待/禁追} — 距关键位 ≤0.5ATR 才可执行
⑨ 复盘闸门：成交必须记录入场、止损、止盈、失效、结果、MAE/MFE
⑩ 心态闸门：禁追价 — 禁复仇 — 连亏后暂停
```

---

## 六类执行模型

### VWAP反抽
```
入场前：4h/1h不冲突 — 价格距VWAP≤0.5ATR — 前段趋势未衰竭
触发：触碰VWAP拒绝 — 5m/15m不重新接受VWAP — Taker/CVD不冲突
确认：顺向K线重启 — 反向量能衰竭 — 回踩/反抽不破确认位
失效：重新接受VWAP — CVD持续背离 — 高周期关键位反向收复
```

### VAH/VAL回收
```
入场前：价格打出价值区外 — 未形成接受 — CVD/Taker未继续放大
触发：价格回到价值区内 — 5m/15m收线确认 — 扫一侧流动性后无法延续
确认：不再跌回/涨回价值区外 — CVD背离或主动成交转向 — 目标至少POC或价值区另一侧
失效：重新突破并接受 — 回踩边界不破后继续顺势 — 回收后量能不足横住
```

### POC拒绝
```
入场前：价格回到POC/HVN — 不是强趋势单边接受 — 附近有拒绝空间
触发：触碰POC失败 — 5m/15m出现拒绝K线或反向主动成交 — POC未被连续收复
确认：拒绝后离开POC区域 — 成交量支持反向 — CVD/Taker不反向冲突
失效：POC被收复并接受 — 回踩POC不破继续原方向 — 围绕POC反复横盘
```

### 扫流动性回收
```
入场前：接近前高/前低/亚洲高低/止损池 — 高周期无强反向压制 — 等主动成交衰竭
触发：刺破关键高低 — 快速回收 — 5m/15m收回关键位内侧
确认：CVD背离或吸收 — Displacement反向确认 — 反向结构位突破
失效：扫后继续放量延续 — 回收失败并在扫点外接受 — 高周期同步破位
```

### 突破接受
```
入场前：接近VAH/VAL/VWAP/前高低 — 高周期支持突破方向 — 突破前无明显衰竭
触发：突破关键位 — 5m/15m收线站稳外侧 — 回踩关键位不破
确认：CVD/Taker顺向且无背离 — OI增加但不过度拥挤 — 回踩后二次启动
失效：突破后快速回落价值区内 — 回踩失败 — CVD背离且Taker追单失败
```

---

## 评分与数据定级

13分制：
① 结构 0-3，权重最高 — 趋势、价值区、关键位是否清晰。
② 周期 -2到2 — 高周期是否支持低周期方向。
③ 订单流/量价 0-2 — CVD、Taker、吸收、背离、成交量是否确认。
④ 衍生/宏观/希腊值 0-2 — 按资产类选择有效因子。
⑤ 催化 0-1 — 新闻、财报、宏观日历需至少两源确认。
⑥ 风控 0-2，权重最高 — R:R、止损、日限、执行距离。
⑦ 情绪 -1到1 — 极端拥挤降权，恐慌小幅加权。

状态判定：
① 10-13：A做多 或 A做空。
② 6-9：B等待。
③ 0-5：X禁做。
④ 数据 C 级：最高 B等待。
⑤ CVD/量价 C 级：订单流分降权，仓位上限半仓。
⑥ 4h 与 1h 同时冲突：X禁做。
⑦ R:R 低于 1:2：X禁做。
⑧ 日损达线、连亏3笔、重大事件窗口：X禁做。

数据定级：
① A：价格多源一致、关键衍生/宏观/量价可用、数据≤15分钟。
② B：价格两源一致，部分因子缺失但不影响核心判断。
③ C：单源、价差异常、关键订单流/宏观/希腊值缺失，最高 B等待。

CVD/量价定级：
① A：逐笔成交或可信订单流源。
② B：Taker买卖量或可靠成交量代理。
③ C：K线估算，只能辅助，不能单独触发。

---

## 监控策略模板

短监控卡用于触发提醒，不替代完整分析卡。

```
◷ {YYYY-MM-DD HH:MM} CST
① 品种：{symbol}
② 周期：{tf} — {状态与原因}
③ 现价：`{price}`
④ 状态：{接近/突破/失效/过期/禁做}
⑤ 模型：{中文模型名/无} · 置信 {n}/5
⑥ 触发位：`{level}` — {触发原因}
⑦ 关键位：阻 `{res}` · 支 `{sup}` · 失效 `{invalid}`
⑧ 订单流：{CVD/成交/宏观/希腊值} — {资产专属确认}
⑨ 风控：{允许/降级/禁止} — 风险 `{risk}U` · R:R {rr}
⑩ 动作：{中文交易建议，必须可执行}
```

监控逻辑：
① 接近信号门槛高于突破信号；接近未确认，突破已有价格确认。
② 突破后结构自动更新必须先落 `monitor_levels.json`，再推送合并消息，避免同一触发推两条。
③ 低位信、数据弱、非关键位订单流只记录不推送。
④ 长驻监控停摆时，5分钟巡检只能补救，不能等同实时监控。
⑤ 自动更新有效的最低证据：`monitor_levels.json` 的 `updated` 改变、关键位改变、刷新队列状态清理、巡检输出成功。

---

## 本地结构化闭环

① 完整卡写入 `data/auto_card_{symbol}.md`。
② 监控位写入 `data/monitor_levels.json`。
③ 计划记录写入 `data/trade_plans.jsonl`。
④ 触发事件写入 `data/monitor_events.json` 与交易事件日志。
⑤ 成交后走成交记录、成交复盘、策略治理。
⑥ 本地 JSON 可保存追踪键；Telegram、分析卡正文、监控卡正文不显示追踪键名。
