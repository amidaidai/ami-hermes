# 棠溪监控警报模板 v6.9.4

定位：短监控卡用于触发提醒，不替代完整分析卡。完整分析卡仍以 `references/master-template-v68.md` 为准。

## 短监控卡

```
◷ {YYYY-MM-DD HH:MM} CST
① 品种：{symbol}
② 周期：{tf} — {状态与原因}
③ 现价：`{price}`
④ 状态：{接近/突破/失效/过期/禁做}
⑤ 模型：{中文模型名/无} · 置信 {n}/5
⑥ 触发位：`{level}` — {触发原因}
⑦ 关键位：阻 `{res}` · 支 `{sup}` · 失效 `{invalid}`
⑧ 订单流：{CVD/成交/宏观/希腊值} — {资产专属确认}
⑨ 风控：{允许/降级/禁止} — 风险 `{risk}U` · R:R {rr}
⑩ 动作：{中文交易建议，必须可执行}
```

## 监控逻辑

① 接近信号门槛高于突破信号；接近未确认，突破已有价格确认。
② 突破后结构自动更新必须先落 `monitor_levels.json`，再推送合并消息，避免同一触发推两条。
③ 低位信、数据弱、非关键位订单流只记录不推送。
④ 长驻监控停摆时，5分钟巡检只能补救，不能等同实时监控。
⑤ 自动更新有效的最低证据：`monitor_levels.json` 的 `updated` 改变、关键位改变、刷新队列状态清理、巡检输出成功。

## 品种路由

① BTCUSDT：Telegram 386。
② XAUUSD：Telegram 385。
③ 信号巡检/系统任务：Telegram 416 或 846，按任务性质分发。
④ 推送目标：Telegram 385/386/846（Feishu sidecar 另行配置）
