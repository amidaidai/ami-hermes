# 双指标字段消费矩阵 · 全面揭露（2026-09-13）

> 审计对象：用户上传定版 `SVP_主指标_空格修正_20260911.pine`（3557 行）
> + `AggVol_副指标_最终版_20260911.pine`（966 行）
> —— 与仓库 `outputs/pine_20260905/SVP_audit_fixed17_20260910.pine`
> / `AggVol_audit_fixed14_20260910.pine` **字节级一致**（diff 空）。
>
> 对照权威：`scripts/tv_indicator_contract.py`（v15，sha256 前 24 位
> 68a34fc32da035880a0b332c / c4c563ef4a08b77cb0ceb73f）。
> 方法：契约全字段 → 全 scripts/ 目录检索（含变体/别名/解码器路径），
> 运行态抽查（data/tv_live_BTCUSDT.json 70 键）。
> 复核脚本：`outputs/audit_20260913/consume_matrix.py`、`consume_verify.py`。

## 总览

| 层 | 数量 | 状态 |
|:--|:--|:--|
| SVP 主指标 DW 字段 | 35 | 31 用上（含多模型链）· 4 未消费 |
| AggVol 副指标 DW 字段 | 34 | 26 用上 · 8 未消费 |
| 主指标行动格 13 行 | 13 | 全部消费（渲染+决策） |
| 副指标行动格 6 行 | 6 | 全部消费（渲染） |
| 契约解码器 | 19 | **全部有调用方** ✓ |
| 契约 ↔ 源码对齐 | — | `tv_indicator_alignment_check.py` 通过 ✓ |

## 一、未消费字段清单（导出了、系统不读）

### A. 冗余型（信息已被等效通道覆盖，可不接）

| 字段 | 等效覆盖 |
|:--|:--|
| MCP Execution Pack | 成分字段（EntryValid/ATR/几何/确认位）均已单独消费 |
| OI Price Direction | Basic Bus `oiDir` 同一信息 |
| HALDRO OI Pack | Basic Bus `oiDir`+`oiAgree` 同一信息 |
| HALDRO Contract Pack | Basic Bus `contract`（22002/22003）已做合同校验 |
| S VWAP ±Band1 | 实盘未用（five_model_matcher 回测链在用）；卡面有本地引擎 σ 带信息 |

### B. 独立信息型（有独特价值，未接 —— 建议评估）

| 字段 | 缓存实值 | 潜在用途 |
|:--|:--|:--|
| OI Breadth | 有值 | 四所多空扩张广度差 → 降权细节 |
| OI Dispersion Ratio | 0.645（9/13 缓存） | 四所 OI 离散度 → 「源分歧」信号 |
| HALDRO Freshness Pack | 0（正常） | 各源新鲜度细分（Stale Venue 已消费粗粒度） |
| HALDRO Flow Pack | — | OI*100+CVD*10+SP 复合流 |
| CVD Anchor Value | — | 锚值本体（卡面可显示「CVD锚」） |
| Coverage Spot / Coverage Perp | — | 覆盖度细分（卡面只显示总数） |
| Exchange Dominance % | 有值 | 交易所占比——**卡面/矩阵未用** |
| DO Price（日开盘） | — | 展示性参考位 |

### C. 仅桥接转存型

| 字段 | 状态 |
|:--|:--|
| MCP Setup Score | 仅 tv_data_bridge 转存进缓存，下游零消费 |

## 二、指标侧「算了没显示」（源码内部，来源：双指标审计报告 20260910）

| 编号 | 内容 |
|:--|:--|
| P1-7 | 总线第 10 位 **CVD 背景码**（cvdBgCodeA）——主指标侧仅 decode 一次无消费者 →「要么接进协同行，要么从合同里去掉」 |
| P2-1 | `SHOW_SPOT_PERP_BASIS` 现货-永续基差——计算了从不显示 |
| P2-4 | AggVol `liqOIDropA`（去杠杆强度）/ volume_buy / volume_sell / coverageRowA / htfTxtA / signalA 算而不显示 |
| P2-3 | 副 S3 冲突下 `visiblePlan` 仍真——风控行与结论行语义冲突（指标侧） |

## 三、全链路 ✓ 的关键消费（节选）

- **SVP 执行主链**：MCP Side/Grade/Entry/Stop/Target/RR/EntryValid/NoTrade 位掩码
  （11 位全表）/Quality/FVG+OB 质量/StructPack/证据时间戳/POC·VAH·VAL·nPOC
  → decision_loop + go_nogo_gate + 卡面
- **副指标确认链**：Valid/State(S0-S4)/CVD/LSR/Volume Ratio/Feed Mode/Composite/
  Stale Venue/Basic Bus → decision_loop 降权与否决、卡面「副指标」行
- **多模型链**：W VWAP / M VWAP → multi_model_engine（月VWAP磁吸等模型）
- **时间戳**：Evidence Bar/Close Time → 触发年龄与新鲜度判定
- **19 个契约解码器**全部有调用方（trigger/regime/contract/evidence/struct/
  quality/bus/feed_mode/oi_presence/no_trade/entry_valid/haldro_state/rr_gate/
  parse_risk_row/ordered_*/risk_row_*）

## 四、处置记录（2026-09-13 已实施，用户批准）

1. ✅ **OI Dispersion** → decision_loop `oi_dispersion_high` 降权（阈值 2.5，
   对齐 AggVol `oiConsensusOk`）；同时修活 `oi_agreement_low` 死逻辑
   —— `dual["oi_present"]` 此前无人写入，Basic Bus 解包现已接入（`auto_card`）。
2. ✅ **CVD Anchor + Bus 背景码** → ③表订单流行「锚x·滚动卖」。
3. ✅ **Exchange Dominance / OI Breadth** → ③表质量行
   「离散x · 主导y% · 广度±n」（主导≥70% 加 ⚠）。
   —— Breadth 说明：指标侧无阈值语义，为避免伪精确仅进展示层，不进降权。
4. ✅ **A 类冗余** → 契约新增「消费状态标注」注释段（存档备查）。
5. ⏳ 指标侧 P1-7（CVD 背景码进协同行）：需下版 Pine 修改，待办。
6. ⏳ DO Price / Coverage 细分 / Freshness / Flow Pack：按需接入（展示性）。

**实测**（2026-09-13 16:41 BTC full）：订单流「CVD🟢买 · 锚-1476.81·滚动卖 · …」
/ 质量「覆盖 5.0 · 质量 8.0 · 离散1.16 · 主导38% · 广度+2 · 风险 无硬风险」。

---
*本矩阵由 2026-09-13 审计生成；后续每次指标升级后重跑 `tv_indicator_alignment_check.py`
+ `outputs/audit_20260913/consume_matrix.py` 复核。*
