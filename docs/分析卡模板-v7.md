# 分析卡模板 v7 · 表格版 + 末尾总结

> 定稿：2026-09-14。用户原话：「这个格式不好，我喜欢表格那种，然后后面有一个总结。」
> 覆盖 v6 面板式（`--style=panel` 仍保留，只是不再是默认）。
> 唯一事实来源仍是 `scripts/auto_card.py` 生成的原生卡；**数字/价位/结论一字不改**，本模板只重排呈现。

## 一、出卡命令

```bash
# 1) 原生卡（事实层）
HANGQING_NO_SEND=1 TANGXI_ENABLE_AUTOMATED_TG=0 \
  python scripts/auto_card.py BTCUSDT --mode-auto --message "分析 BTC"
# 2) 表格版重排（呈现层，默认 tables）
python scripts/card_reformat.py --style=tables data/auto_card_BTCUSDT_full.md
# 3) 同轮截图放回复首行（图片之前不得有文字）
ls -t tools/tradingview-mcp/screenshots/BTCUSDT_15m_*.png | head -1
```

## 二、结构（照抄填空）

```markdown
**{{品种}} {{现价}} · {{HH:MM}} · {{时段}} · {{⚪NO-GO / ⭐GO-A}}**
{{唯一主推一行：⚠️主推 禁做 — 原因 / ⭐主推 — 条件}}

**① 盯什么**

| 状态 | 触发 | 动作 |
|:--|:--|:--|
| ↓ | 失守 {{下方最近位}} | 下看 {{目标1}} → {{目标2}} |
| ↑ | 收上 {{上方最近位}} | 上看 {{目标}} |
| ○ | {{下沿}}–{{上沿}} 之间横盘 | 不动手（现价在此） |

**② 关键位**

| 角色 | 价位 | 距现价 |
|:--|--:|--:|
| {{上沿阻力簇}} | {{价位}} | {{+x.xx%}} |
| ⚖ {{现价所在带}} | {{区间}} | {{±x.xx%}} |
| {{近端转撑}} | {{价位}} | {{-x.xx%}} |
| ⭐{{主观察}} | {{区间}} | {{-x.xx%~-x.xx%}} |

远端 {{层次·结构}} ／ {{层次·结构}} ／ {{层次·结构}}

**③ 多源**

| 源 | 读数 | 裁决 |
|:--|:--|:--|
| SVP主驾驶 | {{…}} | 结构/入场/止损/目标优先 |
| HALDRO副驾驶 | {{…}} | {{降级/否决原因}} |
| 订单流 | {{CVD/OI/费率/恐贪}} | {{…}} |
| 质量 | {{覆盖/离散/广度/风险}} | 覆盖不足不追 |

**总结**

- **最推荐**：{什么方案}。{为什么 —— 人话}
- **看哪里**：看 {上方价}（上方）和 {下方价}（下方）这两个价格。收上 {上方价} 才谈 {上方目标}；跌回 {下方价} 以下就看向 {下方目标}。
- **怎么做**：{下限} 到 {上限} 之间不动作；等 15 分钟收线给出方向、主副指标重新共振后再重算。当前不给入场价，只作人工观察。

注　{{VWAP/EMA/DO 一行}}
　　{{管线路由：N步 · 完成 M/N}}
```

## 三、字段映射（不手填，脚本搬运）

| 位置 | 取自原生卡 | 处理 |
|:--|:--|:--|
| 首行 | 第 1 行 `📊 …` | 压成 `品种 价 时间 时段 徽章` |
| 主推行 | 第 2 行 | 原样 |
| ① 盯什么 | ② 关键位表 | **以现价为锚**：上方最近位＝收上，下方最近位＝失守，中间横盘；目标不足时回退「远端」行 |
| ② 关键位 | ② 关键位表 | 原角色/价位/距现价，按距现价排序，原卡标「主观察」的加 ⭐；远端行单列注脚 |
| ③ 多源 | ③ 多源表 | 原样（4 行） |
| 总结·结论 | `【裁决】` 行 | 原样（FinalVerdict） |
| 总结·三条 | ①/② 推导 | 触发＝三态触发；目标带＝三态目标；未授权＝固定口径 |
| 注 | `VWAP/EMA/DO` 行 + `管线路由` | 原样两行 |

## 四、硬规则

1. **每表 ≤3 列**，表格只用于**比较数字**；键位角色左对齐、价位与距现价右对齐。
2. **同一裁决不写两遍**：主推只在首屏；**总结用大白话三段（最推荐/看哪里/怎么做）**，不带符号串、不复读原生卡的判决行。
3. 首屏到「总结」之间不得出现大段连续文字（一行一个概念）。
4. `⭐` 只在 GO-A 点亮；等待/禁做用 `⚠️主推 禁做` / `🔵主推 等确认`，且**不给 Entry/Stop/Target**，只列人工观察条件（未授权）。
5. 不新增、不改写任何数字/价位/结论；三态由「现价 + 相邻结构位」机械推导。
6. 卡面数字与现场实测不符时，**卡后加一行注明，不改卡**（例：恐贪卡面 69 vs 现场 57）。
7. 附录/源状态/管线审计不占正文；确需审计明细时给 `data/auto_card_<SYM>_full.md` 链接。

## 五、成品示例（2026-09-14 23:20 原生卡重排，未重跑）

```markdown
**BTCUSDT.P 78,536 · 23：20 · 纽约 · ⚪NO-GO**
⚠️主推 禁做 — 副S3冲突·CVD/OI背离

**① 盯什么**

| 状态 | 触发 | 动作 |
|:--|:--|:--|
| ↓ | 失守 78,494–78,544 | 下看 78,336 → 77,747–77,757 |
| ↑ | 收上 78,666 | 上看 79,323（4h·VAH） |
| ○ | 78,544–78,666 之间横盘 | 不动手（现价在此） |

**② 关键位**

| 角色 | 价位 | 距现价 |
|:--|--:|--:|
| 上沿阻力簇·15m·阻 | 78,666 | +0.17% |
| ⚖ 现价所在带·D·VWAP–4h·VWAP | 78,494–78,544 | -0.05%~+0.01% |
| 近端转撑·1h·VAH | 78,336 | -0.25% |
| ⭐主观察·1h·VWAP–15m·VWAP | 77,747–77,757 | -0.99%~-1.00% |

远端 79,323（4h·VAH）／77,442（1h·VAL）／77,213–77,275（4h·POC–D·VAH）

**③ 多源**

| 源 | 读数 | 裁决 |
|:--|:--|:--|
| SVP主驾驶 | C等待 | 结构/入场/止损/目标优先 |
| HALDRO副驾驶 | 偏多 · Composite 1.0 | 副S3冲突·CVD/OI背离 |
| 订单流 | CVD🔴卖 · 锚5388.16·仅前锚 · 主动sell · 费0.0080% · 恐贪69 | CVD/OI不配则降级 |
| 质量 | 覆盖 5.0 · 质量 8.0 · 离散7.72 · 主导41% · 广度-2 · 风险 上级冲突、LSR拥挤 | 覆盖不足不追 |

**总结**

- **最推荐**：等待，不做单。副指标 S3 冲突（主副方向不一致）、CVD 与 OI 背离（量价不一致）。现价就在 78,494–78,544 这个窄带里，没有优势位置。
- **看哪里**：看 78,666（上方）和 78,494–78,544（下方）这两个价格。收上 78,666 才谈 79,323（4h·VAH）；跌回 78,494–78,544 以下就看向 78,336、77,747–77,757。
- **怎么做**：78,544 到 78,666 之间不动作；等 15 分钟收线给出方向、主副指标重新共振后再重算。当前不给入场价，只作人工观察。
```

## 六、历史版式（不再默认）

- v1 三表长源清单 → 「东西太多」；v2 20 行暗号版 → 「看不懂」；v3 可读版 → 「格式有问题」；v4 决策卡 → 「好难看」；
  v5/v6 终端面板式（等宽对齐）→ 「不喜欢，要表格」。
- 面板式仍可用：`python scripts/card_reformat.py --style=panel <卡>`（每行 ≤44 视觉列，CJK 算 2）。
- 审计与对标依据：`outputs/audit_card_format.py`（原生卡 89 行 / 36–72 行超 44 列 / 元信息占 1/3 / 同一裁决重复 9 次）；
  NN/g 渐进披露 · Tufte data-ink · Mission Log 数字等宽右对齐 · NN/g 通知设计。
